# 2026-04-28 今日工作记录

## [10:26] 本轮检查文档

### 本次做了什么
- 将项目阶段文档从 Stage 1 简单选人基线推进到 Stage 2 真实 BP 界面准备期。
- 新增 `stage-02-bp-interface-plan.md`，明确 Stage 2 当前只做本地单机 BP，不扩展赛季、经营、联机房间或完整赛事系统。
- 在 `GameFlowState` 中加入第一版 BP 会话状态：
  - 当前 BP 步骤
  - 双方 Ban 列表
  - 双方 Pick 列表
  - 已 Ban / 已 Pick 英雄不可再选
  - BP 完成后继续生成 `BattleInputConfig`
- 重做 `HeroSelectSceneController` 的第一版 BP 界面结构：
  - 顶部 Stage 2 BP / 阶段提示
  - 左右双方阵容槽
  - 中央英雄池
  - 职业筛选
  - 英雄详情面板
  - 底部禁用区
- 按用户后续修正，将默认 BP 顺序改为：
  - 蓝 B1、红 B1、蓝 B2、红 B2
  - 蓝 P1、红 P1、红 P2、蓝 P2
  - 蓝 P3、红 P3
  - 红 B3、蓝 B3
  - 红 P4、蓝 P4、蓝 P5、红 P5
- 双方 Ban 位从各 2 个改为各 3 个。
- 主菜单文案从 Stage 1 / Start Match 改为 Stage 2 / Start BP。
- 已提交本轮代码与规划文档：`89bab5e Add Stage 2 BP draft flow`。

### 涉及文件
- `AGENTS.md`
- `docs/planning/project-foundation.md`
- `docs/planning/stage-02-bp-interface-plan.md`
- `game/AGENTS.md`
- `game/Assets/Scripts/UI/Flow/GameFlowState.cs`
- `game/Assets/Scripts/UI/Flow/HeroSelectSceneController.cs`
- `game/Assets/Scripts/UI/Flow/MainMenuSceneController.cs`

### 需要检查什么
- 检查 `GameFlowState` 中 `DraftSteps` 是否完全符合用户确认的 BP 顺序。
- 检查双方各 3 Ban、各 5 Pick 的槽位显示和阶段提示是否正确。
- 检查已 Ban / 已 Pick 英雄是否真的无法再次确认。
- 检查 BP 完成前 `Start Battle` 是否保持禁用，BP 完成后是否能进入 Battle。
- 检查 BP 生成的蓝红阵容是否与界面最终 Pick 结果一致。
- 检查 `HeroSelectSceneController` 的 IMGUI 布局在常用 Game 视图分辨率下是否有文字重叠、按钮遮挡或禁用区溢出。
- 检查 Stage 2 文档没有反向改坏 Stage 1 战斗核心边界：BP 只决定参战英雄，不接管战斗胜负、技能、状态、复活或计分。

### 给评审 AI 的重点
- 优先看 `game/Assets/Scripts/UI/Flow/GameFlowState.cs`：
  - `DraftBansPerSide = 3`
  - `DraftSteps` 是否等于用户指定顺序
  - `TryApplyDraftHero`、`CanDraftHero`、`IsHeroBanned`、`IsHeroPicked` 是否没有漏掉禁用 / 已选互斥规则
- 再看 `game/Assets/Scripts/UI/Flow/HeroSelectSceneController.cs`：
  - 当前阶段提示、底部 Ban 区、英雄池遮罩、确认按钮启用条件是否和状态机一致
  - 职业筛选和英雄详情是否只是读取 `HeroDefinition` / `SkillData`，没有写回静态英雄配置
- 对照 `docs/planning/stage-02-bp-interface-plan.md` 检查文档和实现是否一致，尤其是第三轮 Ban 插入点。
- 需要人工进入 `HeroSelect` 实测完整 16 步 BP；本轮没有完成可视化人工操作验证。

### 验证情况
- 已执行 `git diff --check`，未发现空白或行尾错误；仅有 Git 提示后续可能把 LF 替换为 CRLF。
- 使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 运行 batchmode。
- Unity 日志显示已进入脚本编译阶段，`error CS` 数量为 `0`。
- Unity batchmode 没有在 `240` 秒内自然退出，已停止该 batchmode 进程；因此本轮不能声称完整 Unity clean quit。
- 未执行手动点击 HeroSelect 的 16 步 BP 流程验证，后续需要补。

## [14:01] 本轮检查文档

### 本次做了什么
- 完成第二阶段选手系统基础接入，并提交：`f4f2801 接入基础选手系统`。
- 新增独立选手 roster 资源：`game/Assets/Resources/Stage01Demo/Stage02AthleteRoster.asset`，蓝红双方各 5 名选手，共 10 名选手。
- 选手数据不写入英雄资产；BP 和战斗通过 `Resources/Stage01Demo/Stage02AthleteRoster` 读取默认 roster。
- 新增选手相关数据结构：
  - `AthleteDefinition`
  - `AthleteRosterData`
  - `HeroMasteryEntry`
  - `BattleParticipantBinding`
  - `ResolvedAthleteCombatModifier`
- 新增 `AthleteCombatModifierResolver`，按规划公式把“选手 + 英雄”解析成本局战斗修正。
- 接入 BP 流程：
  - `GameFlowState` 读取蓝红双方选手列表。
  - BP 完成后 `BattleInputConfig` 只携带本局临时的选手-英雄绑定结果。
  - `HeroSelectSceneController` 展示选手名、攻防、状态箭头、熟练英雄小头像与适配信息。
- 接入战斗流程：
  - `BattleBootstrapper` 创建运行时英雄时解析选手修正。
  - 如果战斗输入没有携带绑定，则按队伍与槽位从默认 roster fallback。
  - `RuntimeHero` 应用选手修正到 `attackPower`、`maxHealth`、`attackSpeed`、`moveSpeed`。
  - 选手防御只影响最大生命值，没有修改战斗内 `Defense` 减伤属性。
- 接入日志：
  - 新增 `AthleteModifierResolvedEvent`。
  - 战斗日志与 Debug 日志会输出选手绑定、有效攻防、熟练度、修正百分比和适配分。
- 同步更新 `Stage01SampleContentBuilder`，后续生成 demo content 时会生成同一份默认选手 roster。

### 涉及文件
- `docs/planning/stage-02-player-system-plan.md`
- `game/Assets/Resources/Stage01Demo/Stage02AthleteRoster.asset`
- `game/Assets/Scripts/Data/AthleteDefinition.cs`
- `game/Assets/Scripts/Data/AthleteRosterData.cs`
- `game/Assets/Scripts/Data/HeroMasteryEntry.cs`
- `game/Assets/Scripts/Data/BattleParticipantBinding.cs`
- `game/Assets/Scripts/Data/BattleTeamLoadout.cs`
- `game/Assets/Scripts/Data/ResolvedAthleteCombatModifier.cs`
- `game/Assets/Scripts/Core/AthleteCombatModifierResolver.cs`
- `game/Assets/Scripts/Battle/BattleBootstrapper.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleSessionRunner.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Battle/BattleDebugLogForwarder.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/UI/Flow/GameFlowState.cs`
- `game/Assets/Scripts/UI/Flow/HeroSelectSceneController.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`

### 需要检查什么
- 检查 `Stage02AthleteRoster.asset` 是否能在 Unity Inspector 中正常反序列化，且确实包含 10 名选手、每人最多 4 个熟练英雄。
- 检查 `AthleteCombatModifierResolver` 是否严格符合规划：
  - `attack / defense / mastery` 使用 `0 ~ 50`。
  - 熟练度先加到有效攻击和有效防御上。
  - 攻击只影响 `attackPower`。
  - 防御只影响 `maxHealth`，不影响 `RuntimeHero.Defense`。
  - `condition` 只影响攻速和移速。
- 检查 BP 完成后传入 Battle 的是本局 `BattleParticipantBinding`，不是把选手数据写入英雄静态配置。
- 检查直接进入 Battle 场景时 fallback roster 是否按蓝红队和槽位正确绑定。
- 检查 `Stage01SampleContentBuilder` 重新生成内容时不会覆盖用户手调英雄数据之外的无关资源。
- 检查 `HeroSelectSceneController` 当前 IMGUI 布局在常用分辨率下是否有文字重叠、选手槽过密或熟练英雄小头像溢出。
- 写本文档时发现工作区在提交 `f4f2801` 之后仍有未提交的 `game/Assets/Scripts/UI/Flow/HeroSelectSceneController.cs` 布局调整，主要是选手槽高度、特性占位行和熟练英雄头像排版；后续需要确认是否纳入下一次提交。

### 给评审 AI 的重点
- 优先看 `game/Assets/Scripts/Core/AthleteCombatModifierResolver.cs` 和 `game/Assets/Scripts/Heroes/RuntimeHero.cs`，确认公式与 `docs/planning/stage-02-player-system-plan.md` 一致。
- 再看 `game/Assets/Scripts/Battle/BattleBootstrapper.cs`，确认 BP 绑定优先、默认 roster fallback 次之，且没有让 BP UI 直接驱动战斗核心。
- 检查 `game/Assets/Scripts/UI/Flow/GameFlowState.cs`：确认 roster 读取、选手槽初始化、`TryPrepareBattleInput` 的绑定写入都只影响本局 runtime input。
- 检查 `game/Assets/Scripts/UI/Flow/HeroSelectSceneController.cs`：确认显示逻辑只读选手、英雄与解析结果，没有写回静态资产。
- 对照 `game/AGENTS.md` 检查是否仍保持：选手影响本局运行时副本，不修改英雄、技能或选手静态资产。
- 对当前未提交的 `HeroSelectSceneController.cs` diff 做单独判断：如果是用户想保留的 UI 调整，应补验证后单独提交；如果不是本轮目标，应避免混入后续无关提交。

### 验证情况
- 已执行 `git diff --cached --check`，通过。
- 静态检查 `Stage02AthleteRoster.asset`：`athletes=10`，`masteries=40`。
- 使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 运行 batchmode 编译检查。
- Unity 日志出现 `Tundra build success`，未检出 `error CS` 或脚本编译失败。
- Unity 日志包含一批既有第三方 obsolete warning 和 `Newtonsoft.Json.dll` duplicate warning；这些不是本轮新增选手系统脚本错误。
- 最后一轮 batchmode 在 `180` 秒未由命令自然返回，已清理 Unity 进程；日志尾部出现 `Shut down.`，但仍不把它表述为完整人工流程验证。
- 未手动进入 `HeroSelect` 完整走 BP 并进入 Battle；选手槽 UI、绑定通路和战斗内修正仍需要在 Unity 里做一次可视化流程验证。

## [17:08] 本轮检查文档

### 本次做了什么
- 扩展选手特性系统，并提交：`6bcd671 选手特性 扩展数值描述`。
- 在 `AthleteTraitCatalog` 中补齐特性统一显示名与带数值描述，供 BP 展示、战斗日志和调试信息复用。
- 新增并接入 7 个特性：
  - `钟爱蓝色 / favorite_blue`：蓝方选手攻防数值 `+20`，红方 `-5`。
  - `钟爱红色 / favorite_red`：红方选手攻防数值 `+20`，蓝方 `-5`。
  - `疾风步 / wind_step`：英雄移速 `+10%`。
  - `快手 / fast_hands`：英雄攻速 `+10%`。
  - `重盾 / heavy_shield`：选手防御数值 `+20`，英雄移速 `-10%`。
  - `中盾 / medium_shield`：选手防御数值 `+15`，英雄移速 `-5%`。
  - `轻盾 / light_shield`：选手防御数值 `+10`。
- 让 `AthleteCombatModifierResolver.Resolve` 接收 `TeamSide`，BP 当前 Pick 提示和 Battle 创建运行时英雄时都按真实蓝红方结算阵营偏好特性。
- 将护盾类“选手防御提升”接入有效防御数值，再换算到本局最大生命值；没有改英雄战斗内 `Defense` 减伤属性。
- 将移速修正钳制从 `-8% ~ +8%` 放宽到 `-20% ~ +20%`，避免 `疾风步 +10%` 和 `重盾 -10%` 被错误截断。
- 同步更新 demo roster 和 `Stage01SampleContentBuilder`，让 10 名样例选手都带有新旧特性组合。
- 同步更新 `docs/planning/stage-02-player-system-plan.md`，沉淀特性描述、侧方结算、数值通道和最低验证要求。

### 涉及文件
- `docs/planning/stage-02-player-system-plan.md`
- `game/Assets/Resources/Stage01Demo/Stage02AthleteRoster.asset`
- `game/Assets/Scripts/Data/AthleteDefinition.cs`
- `game/Assets/Scripts/Data/ResolvedAthleteCombatModifier.cs`
- `game/Assets/Scripts/Core/AthleteCombatModifierResolver.cs`
- `game/Assets/Scripts/Battle/BattleBootstrapper.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Scripts/UI/Flow/HeroSelectSceneController.cs`

### 需要检查什么
- 检查 `钟爱蓝色`、`钟爱红色` 在 BP 当前选人提示和 Battle 实际创建运行时英雄时是否都带入了正确 `TeamSide`。
- 检查中立预览场景下阵营偏好特性是否不触发加减值，避免错误影响无阵营展示。
- 检查护盾类特性是否只提升选手有效防御数值并影响 `maxHealth`，没有误改英雄战斗内 `Defense`。
- 检查 `疾风步`、`快手` 是否分别进入移速和攻速通道，且与 `condition` 修正叠加后仍按规划钳制。
- 检查 BP 当前选人右侧的小区域是否能容纳新增的特性数值描述，常用分辨率下不要出现明显文字重叠。
- 检查 `Stage02AthleteRoster.asset` 与 `Stage01SampleContentBuilder.cs` 中样例选手特性是否一致，后续重新生成 demo content 不应丢失本轮配置。
- 当前工作区仍有本轮外未提交改动：`AGENTS.md`、`docs/planning/project-foundation.md`、`game/AGENTS.md`，以及未跟踪的英雄文档和今日工作文档；后续提交时不要误混入不相关内容。

### 给评审 AI 的重点
- 优先看 `game/Assets/Scripts/Core/AthleteCombatModifierResolver.cs`：
  - 阵营偏好 `+20 / -5` 是否加到 `traitAttackScoreModifier` 和 `traitDefenseScoreModifier`。
  - 护盾类是否只加 `traitDefenseScoreModifier`，重盾 / 中盾是否同时加移速负修正。
  - 攻速、移速特性是否与 `condition` 修正正确相加并钳制。
- 再看 `game/Assets/Scripts/Data/AthleteDefinition.cs` 和 `ResolvedAthleteCombatModifier.cs`：
  - 每个启用特性是否有带数值 `description`。
  - `traitSummary` / `traitDescriptionSummary` 是否能支持 UI 与日志追踪。
- 检查 `BattleBootstrapper.cs` 与 `HeroSelectSceneController.cs`：
  - Battle 路径是否传入真实阵营。
  - BP 当前 Pick 展示是否使用同一个 resolver，不要出现 UI 与战斗数值不一致。
- 对照 `docs/planning/stage-02-player-system-plan.md` 检查实现和文档一致性，尤其是“选手防御只影响最大生命值，不影响战斗内 Defense”的边界。
- 需要补一次 Unity 内可视化流程验证：走 BP、进入 Battle，观察带特性选手的适配分、战斗日志和侧边栏显示是否符合预期。

### 验证情况
- 已执行 `git diff --check`，通过；仅出现 Git 关于 LF/CRLF 的提示。
- 使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 运行 batchmode 编译检查。
- 第二次 Unity 日志出现 `Tundra build success` 和 `AssetDatabase: script compilation time`，未检出 `error CS`、`Compilation failed` 或 `Unhandled Exception`。
- Unity batchmode 在 `360` 秒内仍未自然退出，已停止本次启动的 Unity 进程；因此本轮只记录为脚本编译日志通过，不记录为完整 clean quit。
- 未手动进入 `HeroSelect` 或 `Battle` 做完整可视化流程验证，UI 容纳性和实战日志表现需要后续补查。

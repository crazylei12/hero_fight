## [11:08] 毒箭手实现与交接

### 本次做了什么
- 结合 `docs/heroes/marksman/venomshooter.md` 与 `docs/planning/stage-01-ultimate-numeric-guidelines.md`，把新射手 `marksman_003_venomshooter` 从文档推进到了 `game/` 内的实际实现。
- 在共享状态/技能层补了 3 组通用能力：`DamageOverTime` 的攻击力快照换算、按指定状态组读取当前叠层、目标死亡后在死亡点触发可连锁的范围后续效果。
- 在 `Stage01SampleContentBuilder` 中新增 Venomshooter 的英雄、普攻附毒、小技能 `Poison Mist`、大招 `Venom Detonation`、英雄目录与样例内容接入。
- 已完成一次代码提交：`f38f114 game: add Venomshooter poison prototype`。
- 当前工作区仍有未提交的文档改动，尤其是 `docs/heroes/marksman/venomshooter.md` 和若干 planning 文档；本次代码提交未包含这些文档。

### 涉及文件
- `docs/heroes/marksman/venomshooter.md`
- `docs/planning/stage-01-ultimate-numeric-guidelines.md`
- `game/Assets/Scripts/Data/StatusEffectData.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Data/UltimateConditionData.cs`
- `game/Assets/Scripts/Data/UltimateConditionType.cs`
- `game/Assets/Scripts/Heroes/RuntimeStatusEffect.cs`
- `game/Assets/Scripts/Heroes/StatusEffectSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattleReactiveGuardSystem.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Logs/codex_venomshooter_generate.log`

### 需要检查什么
- 检查 `Stage01SampleContentBuilder.cs` 与 `venomshooter.md` 的参数是否一致，特别是普攻 `0.6` 倍率、毒层 `0.3 AP / tick`、小技能 `3` 层附毒、大招 `1.5` 倍率/层、尸爆 `3.4` 半径和 `2` 层扩毒。
- 检查共享 `DamageOverTime` 快照实现是否只影响预期范围，尤其是 `RuntimeStatusEffect.cs` 与 `StatusEffectSystem.cs` 的叠层键和刷新逻辑，确认不会误伤现有 HOT、Shield、DamageShare。
- 检查 `BattleSkillSystem.cs` 的按状态层数结算与尸爆链逻辑，确认只统计指定 `stackGroupKey`，无毒目标会跳过，同一单位同一次链条最多触发一次尸爆。
- 检查新增大招条件类型是否与现有 ultimate decision 框架兼容，确认“3 名带毒”/“2 名低血带毒”/`30s` 降到 `2` / `45s` 降到 `1` 的口径成立。
- 本轮没有拿到一次有效的 Unity 编译通过结论，也没有做局内手动战斗验证；这两项都需要后续补查。

### 给评审 AI 的重点
- 优先对照 `docs/heroes/marksman/venomshooter.md` 与 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`，确认设计文档、样例生成器和最终英雄配置没有漂移。
- 重点检查 `game/Assets/Scripts/Heroes/RuntimeStatusEffect.cs`、`game/Assets/Scripts/Heroes/StatusEffectSystem.cs`、`game/Assets/Scripts/Data/StatusEffectData.cs`，确认这次为毒箭手补的共享字段和快照逻辑不会改变其他英雄现有 DOT/HOT 行为。
- 重点检查 `game/Assets/Scripts/Battle/BattleSkillSystem.cs`，确认按层数增伤和死亡点范围跟进效果是共享实现，不是只为毒箭手硬编码的特例。
- 复核 `game/Assets/Scripts/Data/UltimateConditionData.cs` 与 `game/Assets/Scripts/Data/UltimateConditionType.cs`，确认新增条件类型仍符合 `AGENTS.md`、`game/AGENTS.md` 和第一阶段“共享系统优先”的约束。
- review 时注意当前工作区里还有未提交文档改动；不要把 `f38f114` 误认为已经包含所有文档同步。

### 验证情况
- 已执行 `git diff --check` 检查本轮实现文件，没有发现空白符/补丁格式问题。
- 已回读关键 diff 和关键代码段，确认新增字段、调用签名和 builder 接线在文本层面闭合。
- 已尝试使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 运行 batchmode，并执行 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentForBuild`。
- Unity 日志写入到了 `game/Logs/codex_venomshooter_generate.log`，但批处理没有出现 `executeMethod` 完成或编译成功标记，日志停在引擎初始化阶段，未形成可用的编译通过结论。
- 机器上同时存在另一个 Unity 编辑器进程；读取 `Editor.log` 时只能确认编辑器在运行，不能据此认定本轮改动已通过编译。
- 未做局内实机战斗验证，毒层叠加、DOT 跳伤、开大收割和尸爆连锁都需要进入 Unity 场景后补验。

## [15:00] 沙皇第一批实现与交接

### 本次做了什么
- 把 `docs/heroes/mage/sandemperor.md` 的当前方案推进到了 `game/`，这轮先实现共享“部署型固定战斗代理体”能力，没有给沙皇单独加大段专属状态机。
- 新增 `RuntimeDeployableProxy` 与 `BattleDeployableProxySystem`，支持固定位置部署、持续时间、数量上限、超上限顶掉最旧实例、按拥有者普攻触发代理突刺。
- 在共享技能目标类型里新增 `CurrentEnemyTarget`，表达“只认当前普攻目标；目标失效就不放”，用于沙皇小技能的落点选择。
- 把部署物代理接入 `BattleSkillSystem`、`BattleBasicAttackSystem`、`BattleSimulationSystem`、`BattleContext`，并补了 `BalanceSheetService` 的枚举展示。
- 在 `Stage01SampleContentBuilder` 中新增 `mage_003_sandemperor`、小技能 `Raise Sandguard`、大招 `Imperial Encirclement`，并接入英雄目录、技能归属、默认投射物/战斗 prefab 占位。
- 同步补了一条 planning 契约：`docs/planning/stage-01-combat-rules-decisions.md` 新增“当前敌方目标（若当前目标失效则本次不施放）”这一共享技能目标策略说明。
- 已完成一次代码提交：`2f07127 game: add sandemperor deployable proxy prototype`。

### 涉及文件
- `docs/heroes/mage/sandemperor.md`
- `docs/planning/stage-01-combat-rules-decisions.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `game/Assets/Scripts/Battle/BattleDeployableProxySystem.cs`
- `game/Assets/Scripts/Battle/RuntimeDeployableProxy.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattleContext.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillTargetType.cs`
- `game/Assets/Scripts/Editor/BalanceSheetService.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Logs/codex_unity_compile.log`

### 需要检查什么
- 检查 `BattleDeployableProxySystem.cs` 的代理伤害归属是否符合文档：大招生成瞬间那次突刺应记为技能伤害，后续联动突刺应挂回普攻链路。
- 检查代理目标选择与半径判断是否符合文档：优先打拥有者当前普攻目标，不在半径内时改打代理附近最近敌人。
- 检查代理是否真的没有混入英雄集合语义：不参与 `RuntimeHero`、默认索敌、贴身后撤 threat、最小间距分离、body blocking。
- 检查 `CurrentEnemyTarget` 是否只影响沙皇这类明确声明的技能，不会改变现有最近敌人/回退目标的共享行为。
- 检查 `Stage01SampleContentBuilder.cs` 与 `sandemperor.md` 的参数是否一致，重点看普攻 `0.8`、沙兵 `0.55`、联动半径 `2.6`、小技能 `4s CD / 12s 持续 / 5 上限`、大招 `5s` 持续与 `4/3/2` 敌人数量阈值。
- 当前没有实现文档里“大招在有效沙兵数 <= 1 且敌方存活 >= 3 时可提前交”的特判，这一条需要后续继续补。
- 本轮没有拿到一次有效 Unity 编译通过结论，也没有做局内战斗验证；这两项都需要后续补查。

### 给评审 AI 的重点
- 优先检查 `game/Assets/Scripts/Battle/BattleDeployableProxySystem.cs`、`RuntimeDeployableProxy.cs`、`BattleSkillSystem.cs`、`BattleBasicAttackSystem.cs`，确认这次新增的是共享部署物代理系统，不是只为沙皇写死的英雄私有逻辑。
- 对照 `docs/heroes/mage/sandemperor.md` 与 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`，确认样例内容生成器里的英雄 ID、技能 ID、倍率、持续、上限、目标类型没有漂移。
- 重点确认 `BattleSimulationSystem.cs` 与 `BattleContext.cs` 的接入不会影响现有英雄的出生、普攻、技能、死亡复活、60 秒结算与加时决胜流程。
- 重点确认 `CurrentEnemyTarget` 的语义是“当前目标失效则不放”，而不是悄悄退回最近敌人；检查是否仍符合 `AGENTS.md`、`game/AGENTS.md` 和 planning 文档里“共享目标策略优先”的约束。
- review 时注意：本轮 Unity batchmode 验证因本机 Unity 环境问题未形成可靠结论，不能把“没有报错日志”直接当成编译通过。

### 验证情况
- 已执行 `git diff --check`，本轮涉及文件未发现补丁格式或尾随空白问题。
- 已回读关键代码片段，确认新增目标类型、代理运行时、样例生成器接线在文本层面闭合。
- 已尝试使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 运行 batchmode，日志写入 `game/Logs/codex_unity_compile.log`。
- Unity 日志停在编辑器初始化 / 图形设备启动阶段，没有产出编译完成、退出 batchmode 或 `CS` 编译错误段落；因此本轮没有形成可用的 Unity 编译通过结论。
- 用户已明确说明“本机 unity 有点问题，跳过这个环节”，因此本轮后续未继续追打本机 Unity 验证。
- 未做局内手动战斗验证；沙兵生成、普攻联动突刺、大招瞬时重建阵地和旧实例顶替都需要后续进 Unity 场景补验。

## [16:36] 狂战士实现与交接

### 本次做了什么
- 先补了 Berserker 的规则文档与英雄文档，把“被动型小技能占用小技能位”“大招提供短时吸血/体型放大”的口径写入 `docs/planning/` 和 `docs/heroes/warrior/berserker.md`。
- 在共享技能数据层新增 `activationMode`、`passiveSkill`、`temporaryOverride`，让技能可以表达“主动/被动”以及短时覆写型效果。
- 在运行时英雄层补了 3 组共享能力：残血百分比换算攻击力加成、临时吸血比率、临时体型缩放倍率；并保证这些值会随 tick、死亡、重置一起清理。
- 在战斗结算层把被动小技能从主动施法链路中剥离，并把大招临时覆写接进技能结算、伤害后吸血回血、战斗日志与表现层缩放。
- 在 `Stage01SampleContentBuilder` 中新增 `warrior_003_berserker`、被动小技能 `Bloodfury`、大招 `Titan Rage`，并把 Berserker 接入默认英雄目录。
- 已完成一次代码提交：`914e782 game: add Berserker passive hero implementation`。

### 涉及文件
- `docs/heroes/warrior/berserker.md`
- `docs/planning/stage-01-arena-decisions.md`
- `docs/planning/stage-01-combat-rules-decisions.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `game/Assets/Scripts/Data/SkillData.cs`
- `game/Assets/Scripts/Data/SkillActivationMode.cs`
- `game/Assets/Scripts/Data/PassiveSkillData.cs`
- `game/Assets/Scripts/Data/SkillTemporaryOverrideData.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Heroes/StatusEffectSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/BattleDamageSystem.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/UI/BattleView.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`

### 需要检查什么
- 检查 `RuntimeHero.cs` 的残血增伤口径是否符合文档：按缺血比例线性增伤，且上限受 `maxAttackPowerBonus` 限制，不应和现有攻击力状态结算打架。
- 检查 `BattleSkillSystem.cs` 的主动技能判定是否足够严格，确认被动型小技能不会被 AI 当成可施放技能，也不会进入 CD/施法流程。
- 检查 `BattleDamageSystem.cs` 的吸血结算时机是否合理，确认只在实际造成伤害后回血，且不会对护盾吸收、0 伤害或死亡目标产生异常收益。
- 检查 `BattleView.cs` 与临时覆写状态移除逻辑，确认大招结束、死亡、重生后体型会恢复，不会残留缩放状态。
- 检查 `Stage01SampleContentBuilder.cs` 与 `berserker.md` 的参数是否一致，特别是被动最多 `+60%` 攻击力、大招 `6s / +25%` 攻击力 / `+40%` 攻速 / `35%` 吸血 / `1.25x` 体型。
- 本轮没有形成 Unity 编译通过结论，也没有实际生成 Berserker `.asset` 或做局内验证；这些都需要后续补查。

### 给评审 AI 的重点
- 优先检查 `game/Assets/Scripts/Data/SkillData.cs`、`SkillActivationMode.cs`、`PassiveSkillData.cs`、`SkillTemporaryOverrideData.cs`，确认这次新增的是共享技能表达能力，不是只服务 Berserker 的一次性硬编码。
- 重点检查 `game/Assets/Scripts/Heroes/RuntimeHero.cs`、`StatusEffectSystem.cs`、`BattleSkillSystem.cs`、`BattleDamageSystem.cs`，确认被动增伤、临时吸血和体型缩放不会回归现有英雄的主动技能、伤害和状态结算。
- 对照 `docs/heroes/warrior/berserker.md` 与 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`，确认英雄 ID、技能 ID、数值和大招触发条件没有漂移。
- 检查 `BattleSimulationSystem.cs`、`BattleEvents.cs`、`BattleLogSession.cs`、`BattleView.cs`，确认新增日志与表现链路不会刷屏过度，也不会在覆写结束后遗漏状态回收。
- review 时注意：本轮静态闭合已经完成，但 Unity batchmode 验证被本机环境问题中断，不能把“代码已提交”直接等同于“Unity 已验证通过”。

### 验证情况
- 已执行 `git diff --check`，涉及文件没有补丁格式或尾随空白问题，只有 LF/CRLF 提示。
- 已用 `rg` 回扫关键引用，确认 Berserker 数据定义、运行时属性、战斗结算、日志事件和样例生成器接线在文本层面闭合。
- 已回读关键实现文件，静态确认被动技能、临时覆写、吸血和表现缩放调用链已接通。
- 已多次尝试使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 运行 batchmode，并执行 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentBatch`。
- Unity 验证过程中先后遇到本机工程环境损坏：`Packages/manifest.json`、`Packages/packages-lock.json`、`Library/PackageCache/com.unity.2d.sprite.../package.json` 存在非合法 JSON / 缓存损坏问题，导致无法形成可靠的编译和资源生成结论。
- 用户已明确要求放弃本机 Unity 验证，因此本轮最终结论是“代码已写完并已提交，Unity 编译/资源生成未验证”。

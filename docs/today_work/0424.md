## [11:37] 轮子妈实现交接

### 本次做了什么
- 先为新射手英雄“轮子妈”补了设计文档 `docs/heroes/marksman/boomeranger.md`，明确了短射程 AOE 射手定位、普攻弹射、小技能回旋飞轮和大招旋轮风暴。
- 按最终文档口径实现了轮子妈所需的统一能力扩展，而不是写 `heroId` 硬编码特例。
- 普攻系统新增弹射链支持，补了弹射参数、运行时链路状态、事件和日志字段，并预留弹射表现用的 `bounceVariantKey` 接口。
- 技能系统新增固定方向往返路径伤害能力，用于回力镖式小技能；同时接入运行时队列、命中收集和战斗事件。
- 在 `Stage01SampleContentBuilder` 中补齐了轮子妈的英雄配置、普攻参数、小技能、大招和技能引用，并新增编辑器侧枚举显示。
- 完成一次实现提交：`8870bc6 game: add boomeranger aoe marksman`。

### 涉及文件
- `docs/heroes/marksman/boomeranger.md`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattleContext.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/ResolvedBasicAttack.cs`
- `game/Assets/Scripts/Battle/RuntimeBasicAttackProjectile.cs`
- `game/Assets/Scripts/Battle/RuntimeBasicAttackBounceChain.cs`
- `game/Assets/Scripts/Battle/RuntimeReturningPathStrike.cs`
- `game/Assets/Scripts/Data/BasicAttackData.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Editor/BalanceSheetService.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`

### 需要检查什么
- 这轮没有完成一次可靠的 Unity 编译通过验证，需要重新确认项目可正常导入、编译并生成 Demo 内容。
- 需要检查轮子妈普攻弹射是否满足设计约束：超出半径不弹、同一目标最多命中一次、不会形成无限循环。
- 需要检查回旋飞轮的小技能路径判定是否符合预期：出手段、顶点停顿、回收段都按配置生效，且目标筛选没有误伤或漏伤。
- 需要检查大招 `旋轮风暴` 是否是以自身为中心的持续范围伤害，且不会错误复用普攻弹射逻辑。
- 需要检查 `Stage01SampleContentBuilder` 生成的英雄/技能数据是否能被现有 Stage01 资源链正确引用。
- 当前未跑战斗场景手动验证；5v5 出生、索敌、死亡复活、60 秒结算和加时流程没有在这轮里重新确认。

### 给评审 AI 的重点
- 优先检查 `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs` 的弹射链续跳、链结束和目标失效处理，确认不会重复结算、漏发事件或卡住链状态。
- 优先检查 `game/Assets/Scripts/Battle/BattleSkillSystem.cs` 的 returning path strike 队列与结算逻辑，确认其行为和 `docs/heroes/marksman/boomeranger.md` 描述一致。
- 检查 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 里轮子妈的数值、技能引用、释放条件和资源引用，确认遵守 `game/AGENTS.md` 与 Stage01 现有规则。
- 检查新增数据结构 `BasicAttackData.bounce`、`SkillEffectType.CreateReturningPathStrike`、`SkillEffectData` 扩展字段是否会影响现有英雄和编辑器面板。
- 重点补查 Unity 侧未完成验证的部分：用本机编辑器重新导入项目、观察 Console、实际生成或加载 Stage01 Demo 数据。

### 验证情况
- 已做静态检查：`git diff --cached --check` 通过，提交前已清理新增 `.meta` 文件尾部空白。
- 已尝试用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对项目 `G:\BaiduNetdiskDownload\fight\game` 执行 batchmode 生成验证，目标方法为 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContent`。
- 上述 Unity 运行未成功完成；进程在导入/编译前阶段卡住，没有得到成功退出结果，因此这轮不能宣称 Unity 编译通过。
- Unity 日志落在 `game/Logs/codex_boomeranger_generate.log`，可供后续 AI 或人工补查卡住位置。
- 当前工作区仍保留未提交的其他文档改动：`docs/planning/stage-01-hero-spec-decisions.md`、`docs/planning/stage-01-status-effect-decisions.md`、`docs/heroes/mage/lightningmage.md`；这些不在本次提交内。

## [13:55] 雷电法师实现交接

### 本次做了什么
- 先根据 `docs/heroes/mage/lightningmage.md` 和阶段规划文档，把新法师英雄“雷电法师”从文档落到 Unity 实现。
- 新增通用的“伤害触发计数器”能力，而不是给雷电法师写 `heroId` 特判。实现了来自同一施法者的共享计数、阈值触发、眩晕后清零，以及战斗事件与日志输出。
- 在状态系统里补了按 `source + stackGroupKey + statusThemeKey` 精确查询/移除状态的能力，避免多名雷电法师同时上场时互相串计数。
- 在动作序列里补了一个边界修正：全图目标型技能某一跳没有有效目标时不会直接中断，因此雷电法师大招后续仍可命中中途复活的敌人。
- 在 `Stage01SampleContentBuilder` 中新增雷电法师英雄数据、`Thunderline`、`Storm Verdict`、HeroCatalog 接入，并生成了对应 Stage01 Demo 资源。
- 首轮 Unity batchmode 编译时暴露出 `BattleSkillSystem.cs` 里已有的局部变量重名问题（`CS0128` / `CS0165`），已修复后重新执行生成验证。
- 已完成提交：`47c049a game: add lightningmage shock mage`。

### 涉及文件
- `docs/heroes/mage/lightningmage.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `docs/planning/stage-01-status-effect-decisions.md`
- `game/Assets/Scripts/Battle/BattleDamageTriggeredStatusSystem.cs`
- `game/Assets/Scripts/Battle/BattleDamageSystem.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Battle/BattleCombatActionSequenceSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Data/DamageTriggeredStatusCounterData.cs`
- `game/Assets/Scripts/Data/SkillData.cs`
- `game/Assets/Scripts/Data/StatusEffectType.cs`
- `game/Assets/Scripts/Heroes/RuntimeCombatActionSequence.cs`
- `game/Assets/Scripts/Heroes/StatusEffectSystem.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Heroes/mage_004_lightningmage/`
- `game/Assets/Data/Stage01Demo/Skills/mage_004_lightningmage/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 需要确认“感电计数”是否只会在来自同一雷电法师的伤害下累积，不会被其他同主题状态或另一名雷电法师串读。
- 需要确认计数来源覆盖是否符合设计：普攻、小技能命中、大招每次落雷都加层，但阈值触发出来的眩晕不会反向继续叠层。
- 需要确认大招 `Storm Verdict` 的动作序列在“中途无人可打”时只是空过一跳，而不是整段提前结束；后续敌方复活后应还能吃到剩余落雷。
- 需要确认生成器写入的数值与文档一致，尤其是 `0.6s` 眩晕、`3` 层阈值、`4s` 感电持续、`5` 次 `1s` 间隔大招。
- 需要确认默认 `Stage01DemoBattleInput` 没被改阵容，这轮只应新增 HeroCatalog 可选项。
- 这轮没有做战斗场景手动 smoke test，没有实际点进 HeroSelect 或 5v5 对局里看一次完整表现。

### 给评审 AI 的重点
- 先按提交 `47c049a` review，不要把当前工作区里其他未提交改动混进雷电法师实现一起判断。
- 优先检查 `game/Assets/Scripts/Battle/BattleDamageTriggeredStatusSystem.cs`、`game/Assets/Scripts/Heroes/StatusEffectSystem.cs`、`game/Assets/Scripts/Battle/BattleDamageSystem.cs`，确认共享计数、阈值触发、清层和来源过滤都正确。
- 检查 `game/Assets/Scripts/Battle/BattleCombatActionSequenceSystem.cs` 与 `game/Assets/Scripts/Heroes/RuntimeCombatActionSequence.cs`，确认新增的“空跳过但不中断”只影响全图目标动作序列，不会回归其他连段英雄。
- 检查 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 和生成出的 `game/Assets/Data/Stage01Demo/Skills/mage_004_lightningmage/*.asset`，确认技能目标类型、动作序列、计数器配置与文档一致。
- 检查 `docs/planning/stage-01-hero-spec-decisions.md`、`docs/planning/stage-01-status-effect-decisions.md` 与实现是否保持一致，确认这套机制仍然是统一系统能力，不是英雄私有底层。
- 当前工作区还有未提交的其他改动，例如 `docs/heroes/marksman/boomeranger.md`、`docs/heroes/tank/mundo.md` 以及几处被动自疗/状态相关脚本；这些不是本轮雷电法师提交内容，评审时应分开看。

### 验证情况
- 已用本机 Unity 编辑器 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对项目 `G:\BaiduNetdiskDownload\fight\game` 执行 batchmode，目标方法为 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContent`。
- 首轮 batchmode 发现编译错误：`game/Assets/Scripts/Battle/BattleSkillSystem.cs` 中出现 `CS0128` 与 `CS0165`，已在本轮修复后重新执行。
- 二次执行已成功退出，日志见 `game/Logs/codex_lightningmage_generate.log`；日志中可见 `mage_004_lightningmage/Lightningmage.asset`、`Thunderline.asset`、`Storm Verdict.asset` 被导入，且有 `Batchmode quit successfully` / `Exiting batchmode successfully now!`。
- 已生成并提交雷电法师相关 Stage01 Demo 资源与脚本改动，提交号为 `47c049a`。
- 未执行手动对局验证、未录 battle log、未逐项观察眩晕链在真实 5v5 中的表现。

## [14:51] 蒙多实现交接

### 本次做了什么
- 将新坦克英雄“蒙多”从设计文档落到 Stage01 运行时与 Demo 资源。
- 在共享战斗链路中补了两项通用能力：
  - `PassiveSkillData` / `RuntimeHero` 支持按当前血线分段结算的定时自我治疗。
  - `StatusEffectData` / `RuntimeStatusEffect` 支持按目标最大生命值结算 `HealOverTime` 数值。
- 在 `BattleSimulationSystem` 中接入被动定时回血触发，让蒙多被动和大招都走统一治疗入口，并受目标侧 `HealTakenModifier` 影响。
- 生成并接入了 `tank_004_mundo`、`Brute Metabolism`、`Monstrous Recovery` 的 Stage01 Demo 资源，并把蒙多加入 `Stage01HeroCatalog`。
- 由于当前 HeroCatalog 也引用了 `marksman_004_boomeranger`，本轮一并补交了此前仓库缺失的 boomeranger Stage01 Demo 资源目录，避免目录校验或资源引用悬空。
- 默认 `Stage01DemoBattleInput` 阵容未切到蒙多，本轮只新增 HeroCatalog 可选项。
- 已完成提交：`d2d8c80 game: add mundo sustain tank and sync demo assets`。

### 涉及文件
- `docs/heroes/tank/mundo.md`
- `game/Assets/Scripts/Data/PassiveSkillData.cs`
- `game/Assets/Scripts/Data/StatusEffectData.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Heroes/RuntimeStatusEffect.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`
- `game/Assets/Data/Stage01Demo/Heroes/tank_004_mundo/`
- `game/Assets/Data/Stage01Demo/Skills/tank_004_mundo/`
- `game/Assets/Data/Stage01Demo/Heroes/marksman_004_boomeranger/`
- `game/Assets/Data/Stage01Demo/Skills/marksman_004_boomeranger/`

### 需要检查什么
- 这轮没有做手动对局 smoke test，需要实际进一次 5v5 战斗确认蒙多在前排的站场、死亡复活和大招释放表现是否符合预期。
- 需要确认被动三档回血是否严格按当前生命值比例实时切换，且在 `>60%`、`30%~60%`、`<=30%` 三段边界没有跳档错误。
- 需要确认 `HealTakenModifier` 会同时影响蒙多被动与大招，尤其是 `-1.0` 禁疗时是否能完全阻断回复。
- 需要确认大招当前实现的近似条件与文档补充说明一致：默认 `<=45%` 血且 `6.5` 范围内至少 `2` 名敌人；开战 `45s` 后放宽为 `<=60%` 血且至少 `1` 名敌人。
- 需要确认 `Stage01HeroCatalog.asset` 只新增可选英雄，不会意外改动默认 BattleInput 阵容。
- 这轮顺带提交了 boomeranger 的 Demo 资产目录，但没有围绕它重新做行为验证；若评审覆盖到资源层，需确认这部分只是资源补齐而非逻辑回归。

### 给评审 AI 的重点
- 先按提交 `d2d8c80` review，不要把当前工作区里仍未提交的旧改动混进这轮实现一起判断。当前仍有未提交项：`docs/heroes/marksman/boomeranger.md`，以及几份旧英雄 asset。
- 优先检查 `game/Assets/Scripts/Data/PassiveSkillData.cs`、`game/Assets/Scripts/Data/StatusEffectData.cs`、`game/Assets/Scripts/Heroes/RuntimeHero.cs`、`game/Assets/Scripts/Heroes/RuntimeStatusEffect.cs`、`game/Assets/Scripts/Battle/BattleSimulationSystem.cs`，确认新增能力是共享系统扩展，不会回归现有治疗、状态或复活流程。
- 检查 `game/Assets/Data/Stage01Demo/Skills/tank_004_mundo/*.asset` 与 `game/Assets/Data/Stage01Demo/Heroes/tank_004_mundo/Mundo.asset` 是否和 `docs/heroes/tank/mundo.md` 当前口径一致，尤其是被动三档数值、大招 HOT 配置和一次性大招定位。
- 检查 `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset` 的新增引用是否完整，并确认默认 `Stage01DemoBattleInput.asset` 没被这轮误改。
- 如果要补查资源可再生性，建议顺手对照当前 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`，确认生成器状态与已提交资源一致；但这份生成器文件不在本次提交里。

### 验证情况
- 已先尝试在沙箱内运行 Unity batchmode，但该次失败原因是环境权限与本地 Package Manager IPC，不是项目脚本编译错误。
- 随后已用本机 Unity 编辑器 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对项目 `G:\BaiduNetdiskDownload\fight\game` 提权执行 batchmode 生成验证，目标方法为 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentBatch`。
- 生成验证成功，日志见 `game/Logs/codex_mundo_generate.log`；日志中可见 `tank_004_mundo/Mundo.asset`、`Brute Metabolism.asset`、`Monstrous Recovery.asset` 被导入，且有 `Batchmode quit successfully` / `Exiting batchmode successfully now!`。
- 已再执行一次纯打开/编译检查，日志见 `game/Logs/codex_mundo_compile.log`，退出码为 `0`，未出现新的 C# 编译错误。
- 编译日志中仍有现有项目的非阻塞提示，例如包更新 SSL 报错、shader fallback、TMP font `Units Per EM` 序列化提醒；这些不是本轮新增错误。
- 已完成提交 `d2d8c80`；未执行手动 HeroSelect / Battle 场景点击验证，也未录制实际 battle log。

## [16:06] 厨子实现交接

### 本次做了什么
- 将新辅助英雄“厨子”从设计文档落到 Stage01 运行时与 Demo 资源，定位为偏团队持续回复的供给型辅助。
- 普攻侧新增“优先给当前没有对应持续效果或效果将到期的友军补覆盖”的目标能力，用于 `披萨` 持续回复。
- 技能系统新增“施法开始时锁定随机技能变体”的统一运行时承载，支持 `Daily Special` 在汉堡 / 烤肠 / 薯条三种增益里随机其一后，再按团队累计伤害或承伤挑目标。
- AI 目标系统新增“施法范围内累计造成伤害最高的友军”和“施法范围内累计承受伤害最高的友军”两种共享目标类型。
- 大招 `Grand Feast` 已实现为全体友军即时治疗 + 持续回复 + 攻防提升，并补了大招回退模板对次级条件切换的支持。
- 同步补充了 `docs/heroes/support/chef.md`，并把相关统一规则写回 `docs/planning/stage-01-combat-rules-decisions.md`、`docs/planning/stage-01-hero-spec-decisions.md`。
- 已完成提交：`c264968 game: add chef sustain support hero`。

### 涉及文件
- `docs/heroes/support/chef.md`
- `docs/planning/stage-01-combat-rules-decisions.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `game/Assets/Scripts/Battle/BattleAiDirector.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/ResolvedSkillCast.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Data/BasicAttackData.cs`
- `game/Assets/Scripts/Data/SkillData.cs`
- `game/Assets/Scripts/Data/SkillTargetType.cs`
- `game/Assets/Scripts/Data/UltimateFallbackData.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Heroes/StatusEffectSystem.cs`
- `game/Assets/Data/Stage01Demo/Heroes/support_005_chef/`
- `game/Assets/Data/Stage01Demo/Skills/support_005_chef/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 需要补做一次手动对局 smoke test，确认厨子在真实 5v5 中的普攻补 HOT、随机小技能投喂和大招全队增益表现都符合文档。
- 需要确认普攻“缺失或即将过期优先补覆盖”的逻辑不会错误跳过低血量友军，也不会在已有充足持续时间时反复浪费目标切换。
- 需要确认 `Daily Special` 的随机变体是在施法开始时锁定，而不是在飞行物命中或结算时重新随机。
- 需要确认“最高输出友军 / 最高承伤友军”读取的是统一运行时累计统计，不会被英雄私有状态串改。
- 需要确认 `Grand Feast` 的大招回退条件切换只影响本轮新增的显式字段，不会回归其他已有大招模板。
- 这轮没有完成一次可通过的 Unity 全项目编译验证；当前 batchmode 被仓库里已有的 `Stage01SampleContentBuilder.cs` 中 `Spellblade` 缺失函数阻塞，需要后续单独补查。

### 给评审 AI 的重点
- 优先检查 `game/Assets/Scripts/Battle/BattleSkillSystem.cs`、`game/Assets/Scripts/Battle/ResolvedSkillCast.cs`、`game/Assets/Scripts/Data/SkillData.cs`，确认随机变体选择、传递、日志记录和结算使用的是同一个已锁定版本。
- 优先检查 `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs` 与 `game/Assets/Scripts/Heroes/StatusEffectSystem.cs`，确认友军普攻补覆盖逻辑只依赖统一状态系统，不会引入新的英雄特判。
- 检查 `game/Assets/Scripts/Battle/BattleAiDirector.cs` 与 `game/Assets/Scripts/Data/SkillTargetType.cs`，确认“最高输出 / 最高承伤友军”筛选规则和规划文档新增条目一致。
- 检查 `game/Assets/Data/Stage01Demo/Heroes/support_005_chef/Chef.asset`、`game/Assets/Data/Stage01Demo/Skills/support_005_chef/*.asset` 与 `docs/heroes/support/chef.md` 是否一致，尤其是 HOT、Buff 数值、目标类型和大招释放条件。
- 评审时请把当前未提交的其他工作区改动排除在外，尤其是 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 的 `Spellblade` WIP；那部分不是本轮 `厨子` 提交内容。

### 验证情况
- 已用本机 Unity 编辑器 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对项目 `G:\BaiduNetdiskDownload\fight\game` 执行 batchmode 编译检查，日志见 `game/Logs/codex_unity_compile_chef.log`。
- 首轮编译暴露出本轮直接引入的问题：`game/Assets/Scripts/Data/SkillData.cs` 缺少 `using System;`；已在本轮修复。
- 修复后再次执行 batchmode，未再出现本轮 `厨子` 改动直接导致的新编译错误。
- 当前剩余阻塞为仓库已有的 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 中 `CreateSpellbladeActiveSkill`、`CreateSpellbladeUltimateSkill`、`ConfigureSpellbladeUltimate`、`ConfigureSpellbladeBasicAttack` 缺失，属于外部未完成工作，不在本轮提交内。
- 未执行 HeroSelect / Battle 场景手动验证，未录制 battle log。

## [17:01] 魔剑客实现交接

### 本次做了什么
- 将新战士英雄“魔剑客”从 `docs/heroes/warrior/spellblade.md` 落到 Stage01 代码与生成器。
- 在 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 中新增 `warrior_004_spellblade`、小技能 `裂空剑气`、大招 `缚阵魔剑` 的基础数值、技能配置、大招释放条件和 `HeroCatalog` 接入，并修正过一次生成器里误漏的 `chef` 目录项。
- 在 `game/Assets/Scripts/Data/SkillEffectData.cs` 为部署物代理新增 `DeployableProxyTriggerMode.PeriodicEffectPulse`，用于“固定魔剑每 1 秒脉冲一次”的通用触发模式。
- 新增 `game/Assets/Scripts/Battle/BattleForcedMovementUtility.cs`，把强制位移扩成“朝任意锚点拉近”的共享能力，供部署物锚点复用，并保留最小间距/合法落点分配逻辑。
- 在 `game/Assets/Scripts/Battle/BattleDeployableProxySystem.cs` 与 `game/Assets/Scripts/Battle/RuntimeDeployableProxy.cs` 接入部署物周期脉冲、延后首跳和目标筛选逻辑，让魔剑大招按 `1s` 间隔离散拉拽周围敌人。
- 在 `game/Assets/Scripts/Battle/BattleEvents.cs`、`game/Assets/Scripts/Battle/BattleLogSession.cs` 补了 `DeployableProxyPulseEvent`，使魔剑大招具备“生成 / 脉冲 / 单体拉拽 / 消失”可观测链路。
- 已完成提交：`5d93896 game: add spellblade warrior hero`。

### 涉及文件
- `docs/heroes/warrior/spellblade.md`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Battle/BattleForcedMovementUtility.cs`
- `game/Assets/Scripts/Battle/BattleDeployableProxySystem.cs`
- `game/Assets/Scripts/Battle/RuntimeDeployableProxy.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`

### 需要检查什么
- 这轮没有完成一次可确认成功的 Unity 生成或对局验证，需要重新确认项目导入、编译和 Stage01 Demo 内容生成是否正常。
- 需要确认 `PeriodicEffectPulse` 只影响预期的部署物脉冲逻辑，不会回归已有的 `PeriodicBasicAttackSequence` 代理体。
- 需要确认“朝部署物锚点拉近”的共享强制位移在多人同时被拉时不会发生重叠、抖动、卡边界或越界落点。
- 需要确认魔剑大招的首个脉冲是在部署后 `1s` 触发，而不是落地瞬间触发。
- 需要确认 `Stage01SampleContentBuilder.cs` 中魔剑客的数值、技能描述、HeroCatalog 接入与 `docs/heroes/warrior/spellblade.md` 一致，且没有再误伤其他英雄目录项。
- 这轮没有生成并提交 `game/Assets/Data/Stage01Demo/Heroes/warrior_004_spellblade/` 与 `game/Assets/Data/Stage01Demo/Skills/warrior_004_spellblade/` 资源目录；后续若跑生成器，需要确认资源可正常产出且引用稳定。

### 给评审 AI 的重点
- 先按提交 `5d93896` review，不要把当前工作区里未提交的其他改动混进来。当前仍有独立脏文件：`docs/heroes/marksman/boomeranger.md`、几份旧英雄 asset、`docs/today_work/0424.md`、`skill_diff.txt`。
- 优先检查 `game/Assets/Scripts/Battle/BattleForcedMovementUtility.cs`、`game/Assets/Scripts/Battle/BattleDeployableProxySystem.cs`、`game/Assets/Scripts/Battle/RuntimeDeployableProxy.cs`、`game/Assets/Scripts/Data/SkillEffectData.cs`，确认“部署物脉冲 + 锚点拉拽”是共享系统扩展，不是英雄私有硬编码。
- 检查 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 中魔剑客的小技能路径伤害、大招部署物配置、AI 释放条件和 `HeroCatalog` 接入，确认既符合 `game/AGENTS.md` 的 Stage01 范围，也和英雄文档一致。
- 检查 `game/Assets/Scripts/Battle/BattleEvents.cs` 与 `game/Assets/Scripts/Battle/BattleLogSession.cs`，确认日志侧至少能覆盖魔剑生成、每次脉冲、每个目标被拉动和代理体结束。
- 若评审要补运行时验证，建议直接用本机 Unity 重新导入并生成一次 Demo 内容，再实际进一次 5v5 对局观察魔剑客小技能路径命中和大招脉冲拉阵表现。

### 验证情况
- 已做提交前静态检查，并完成提交 `5d93896 game: add spellblade warrior hero`。
- 本地存在 `game/Logs/codex_spellblade_generate.log`；日志里能看到脚本编译与部分资源导入信息，但当前未见 `Batchmode quit successfully` 或 `Exiting batchmode successfully now!` 成功退出标记，因此这轮不能宣称 Unity 生成验证通过。
- 按用户最终要求，这轮没有继续执行 Unity batchmode 或手动对局验证。
- 当前提交只包含代码与文档，不包含新生成的 `spellblade` Stage01 Demo 资源目录。

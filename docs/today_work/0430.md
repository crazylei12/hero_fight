## [10:31] 本轮检查文档

### 本次做了什么
- 完成镜法师 `Mirrormage / 镜法师` 的文档、共享分身机制代码、生成数据和提交。
- 新增共享 `CloneUnit / 临时分身单位` 运行时系统，支持复制敌方正式英雄为创建者阵营的临时友方单位。
- 镜像体可以移动、索敌、普攻、使用 clone-safe 小技能、被选中、受伤、受控、被击杀或到时消失。
- 镜像体不能释放大招，不能创建新的分身、召唤物或部署体，不进入死亡复活流程，死亡不增加击杀分。
- 按用户修正，镜像体强度只通过生命、攻击、防御、攻速、移速等属性缩减体现，不再对小技能效果做统一二次降低。
- 提交已生成：`1da9e46 实现镜法师`。

### 涉及文件
- `docs/heroes/mage/mirrormage.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `docs/planning/stage-01-combat-rules-decisions.md`
- `game/Assets/Scripts/Battle/BattleCloneSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Battle/BattleDamageSystem.cs`
- `game/Assets/Scripts/Battle/BattleStatsSystem.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Battle/BattleDebugLogForwarder.cs`
- `game/Assets/Scripts/Battle/BattleSessionRunner.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Scripts/Editor/BalanceSheetService.cs`
- `game/Assets/Scripts/UI/BattleView.cs`
- `game/Assets/Scripts/UI/BattleSideHeroSidebarHud.cs`
- `game/Assets/Data/Stage01Demo/Heroes/mage_006_mirrormage/`
- `game/Assets/Data/Stage01Demo/Skills/mage_006_mirrormage/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 检查 `BattleCloneSystem` 的分身生成、目标过滤、上限替换、持续时间清理、创建者死亡清理是否符合文档规则。
- 检查 `BattleSkillSystem` 中 clone-safe 小技能判定是否过严或过松，尤其是禁止分身、召唤、部署体、大招、一局一次资源、复活、永久成长等效果的规则。
- 检查分身击杀正式英雄时的计分、归属、日志、统计是否同时保留实际行动单位和创建者归属。
- 检查分身死亡或消失不进入复活流程、不增加击杀分、不污染最终 `heroStats`。
- 检查 UI 动态创建和移除分身视图是否稳定，避免分身消失后残留不可见可索敌对象或残留视图。
- 检查镜法师小技能和大招生成资产中的属性缩减值是否符合当前第一阶段数值体系，尤其确认没有额外的技能效果缩减字段。
- 检查 `Stage01SampleContentBuilder` 是否只引入镜法师相关生成内容，没有把其它英雄资产误纳入本轮范围。

### 给评审 AI 的重点
- 优先看 `game/Assets/Scripts/Battle/BattleCloneSystem.cs`、`BattleSkillSystem.cs`、`RuntimeHero.cs` 三处，这是分身机制的主链路。
- 对照 `docs/planning/stage-01-hero-spec-decisions.md` 与 `stage-01-combat-rules-decisions.md`，确认代码语义与共享 `CloneUnit` 规则一致。
- 对照 `docs/heroes/mage/mirrormage.md`，确认镜法师小技能是单体复制，大招是多目标复制，并且镜像体技能不做二次效果缩减。
- 重点复查边界：复制镜法师、复制召唤英雄、复制已有分身、分身被击杀、创建者死亡、分身刚生成的同帧行动、战斗结束统计。
- 当前写交接时观察到工作区另有未纳入本轮提交的变化：`docs/planning/stage-01-hero-spec-decisions.md` 和 `docs/heroes/mage/marisa.md`。这些不是本次交接文档要处理的内容，评审时应先确认是否为用户后续编辑。

### 验证情况
- 已执行 `git diff --cached --check`，提交前通过；生成 Unity YAML 里的尾随空格已清理。
- 已执行 Unity batchmode：`Stage01SampleContentBuilder.GenerateDemoContent`。
- Unity 日志包含 `Tundra build success`、`Batchmode quit successfully invoked`、`Exiting batchmode successfully now!`。
- Unity 日志中存在第三方资源包 feedback 弹窗和一次 licensing access token 噪声，但没有阻塞 batchmode 成功退出。
- 已检查镜法师生成技能资产，`Mirror Copy` 与 `Hall of Reflections` 使用 `CreateCloneUnit`，配置了持续时间、生成数量、上限和属性倍率。
- 未做完整手动观战回放或专门的战斗用例测试；评审 AI 需要补查实战表现和边界回归。

## [11:24] 本轮检查文档

### 本次做了什么
- 完成法师英雄 `Marisa / 雾雨魔理沙` 从文档到 Unity 数据与运行时支持的落地。
- 新增共享 `CreateChanneledPathDamage` / `RuntimeChanneledPathSkill` 能力，用来表达蓄力后持续引导、可缓慢转向、按 tick 命中直线路径的魔炮类技能。
- 接入魔理沙样例数据：普攻 `Star Shot`、小技能 `Broom Burst`、大招 `Starlance Cannon`，并加入 `Stage01HeroCatalog`。
- `Broom Burst` 采用远离当前目标的 3.2 世界单位短距扫帚位移，并附带一轮星弹径向扫射。
- `Starlance Cannon` 采用一局一次的高伤害持续直线魔炮：0.85 秒蓄力，2.8 秒引导，0.35 秒 tick，单 tick 攻击力倍率 3.30，满额吃满按可击杀坦克的持续伤害大招模板设计。
- 已提交：`6c8cc62 Add Marisa mage hero`。

### 涉及文件
- `docs/heroes/mage/marisa.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Battle/RuntimeChanneledPathSkill.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/BattleContext.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Battle/BattleDamageTriggeredStatusSystem.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Scripts/Editor/BalanceSheetService.cs`
- `game/Assets/Data/Stage01Demo/Heroes/mage_007_marisa/`
- `game/Assets/Data/Stage01Demo/Skills/mage_007_marisa/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 检查 `CreateChanneledPathDamage` 是否确实是共享机制，而不是只对魔理沙硬编码；后续光束、魔炮、扫射激光是否能复用同一数据结构。
- 检查 `RuntimeChanneledPathSkill` 的蓄力、tick 计时、结束原因、动作锁、缓慢转向和路径收集是否符合文档边界。
- 检查 `Starlance Cannon` 的伤害是否符合持续伤害型大招模板：满额吃满允许击杀坦克，但实战应因蓄力、引导、转向速度、站位和打断而不稳定打满。
- 检查 `Broom Burst` 的 `repositionAwayFromPrimaryTarget` 是否只让魔理沙拉开距离，不会变成穿后排或刺客式切入。
- 检查魔炮 tick 的 `DamageSourceKind.ChanneledPathSkillTick` 是否应继续按技能伤害触发相关计数与日志，不要误算成普攻或 DOT。
- 检查生成资产中的路径长度、宽度、tick 间隔、倍率、蓄力时间和转向速度是否与文档一致。

### 给评审 AI 的重点
- 优先看 `BattleSkillSystem.cs`、`RuntimeChanneledPathSkill.cs`、`SkillEffectData.cs` 和 `SkillEffectType.cs`，确认新效果类型的运行时主链路没有破坏已有技能效果。
- 对照 `docs/heroes/mage/marisa.md` 与 `stage-01-hero-spec-decisions.md`，确认魔理沙不是控制法师、刺客或前排，而是中后排持续弹幕 + 一局一次直线爆发法师。
- 重点复查边界：施法者死亡、硬控、被强制位移、目标死亡、目标走位、路径转向速度、引导结束最后一 tick、战斗结束时 channel 清理。
- 复查 `Stage01SampleContentBuilder.cs` 和生成的 `.asset` 文件是否一致，避免以后重新生成内容时覆盖掉手工修正。
- 当前工作区仍有未纳入本轮提交的其它改动：`docs/heroes/mage/mirrormage.md`、若干分身相关 Battle 文件、`docs/heroes/tank/blazeroar.md`。这些不属于魔理沙提交，评审时应分开处理。

### 验证情况
- 已执行 Unity batchmode：`Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentBatch`。
- Unity 日志包含 `Tundra build success (30.70 seconds)`，魔理沙英雄与技能 `.asset` 已生成。
- 已检查生成资产关键值：`Starlance Cannon` 使用 `effectType: 15`、`powerMultiplier: 3.3`、路径长度 `20`、宽度 `2.4`、蓄力 `0.85`、转向速度 `35`；`Broom Burst` 使用 `forcedMovementDistance: 3.2` 与 `repositionAwayFromPrimaryTarget: 1`。
- 已执行 `git diff --cached --check`，提交前通过；本次新增 Unity YAML 的尾随空格已清理。
- Unity batchmode 没有干净退出，日志未出现明确 clean quit 标记，进程在退出阶段无进展后被强制停止；因此不能把这次验证描述为完整 clean quit。
- 未做完整手动观战回放、自动战斗用例或专门的平衡回归测试；评审 AI 需要补查实战表现。

## [12:11] 本轮检查文档

### 本次做了什么
- 完成坦克英雄 `Blazeroar / 炽焰咆哮虎` 从文档到 Unity 数据与运行时支持的落地。
- 新增普攻目标切换首击机制：切换到新敌方目标后的第一次普攻可以使用较低伤害倍率，并附加首击状态；炽焰咆哮虎配置为 `0.45` 倍伤害、`0.65` 秒眩晕、同目标 `2.5` 秒触发间隔。
- 新增被动出生 / 复活触发状态机制：`PassiveSkillData` 可配置开局首次出生与复活时对敌方英雄施加状态；炽焰咆哮虎配置为敌方全体 `AttackPowerModifier -18%`，持续 `4` 秒。
- 接入炽焰咆哮虎小技能 `Blazing Roar`：主动释放时远离当前目标后撤 `3.0`，并对当前目标施加 `AttackPowerModifier -30%`，持续 `4` 秒。
- 接入炽焰咆哮虎大招 `Inferno Rush`：先自疗，再朝当前目标方向冲锋，路径敌人受到伤害；当前资产用 `DirectHeal` 攻击力倍率折算为基础最大生命值的 `25%`。
- 更新 `Stage01SampleContentBuilder` 与生成资产，将 `tank_007_blazeroar` 加入 `Stage01HeroCatalog`。
- 已提交：`c40cf91 实现炽焰咆哮虎`。

### 涉及文件
- `docs/heroes/tank/blazeroar.md`
- `game/Assets/Scripts/Data/BasicAttackData.cs`
- `game/Assets/Scripts/Data/PassiveSkillData.cs`
- `game/Assets/Scripts/Battle/ResolvedBasicAttack.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattlePassiveSpawnTriggerSystem.cs`
- `game/Assets/Scripts/Battle/BattleSessionRunner.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Heroes/tank_007_blazeroar/`
- `game/Assets/Data/Stage01Demo/Skills/tank_007_blazeroar/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 检查目标切换首击是否只在真实敌方目标变化后触发，不会因为普通索敌反复评估或目标失效变成连续眩晕。
- 检查同一目标 `2.5` 秒冷却是否按目标 `RuntimeId` 生效，并确认死亡、复活、目标清空时不会残留错误首击状态。
- 检查开局首次出生触发顺序：所有英雄的 `UnitSpawnedEvent` 已先发布，再统一结算出生被动；确认不会出现状态事件早于目标单位出生事件的日志 / UI 顺序问题。
- 检查出生 / 复活减攻是否只作用于敌方正式英雄，不应影响分身、召唤物、部署体、死亡单位或友方单位。
- 检查 `Inferno Rush` 的自疗实现是否需要后续改成真正的最大生命值百分比治疗；当前是按炽焰咆哮虎基础面板折算的 `DirectHeal` 倍率，不会随最大生命值临时变化自动调整。
- 检查生成资产与 `Stage01SampleContentBuilder` 是否一致，避免下次重新生成时覆盖手工修正。

### 给评审 AI 的重点
- 优先看 `RuntimeHero.cs`、`BattleBasicAttackSystem.cs`、`ResolvedBasicAttack.cs`，确认目标切换首击的挂起、消耗、低伤害替换和眩晕状态合并没有破坏普通普攻、弹射、命中特效或已有 on-hit 状态。
- 优先看 `BattlePassiveSpawnTriggerSystem.cs`、`BattleSessionRunner.cs`、`BattleSimulationSystem.cs`、`PassiveSkillData.cs`，确认开局出生和复活被动是共享能力，不是炽焰咆哮虎硬编码。
- 对照 `docs/heroes/tank/blazeroar.md` 检查小技能被动群体减攻是否包含开局首次出生，并确认同目标首击眩晕冷却已是 `2.5` 秒。
- 重点复查边界：炽焰咆哮虎死亡后复活、敌方有人死亡 / 复活、目标切换到刚复活敌人、分身存在时的过滤、首击没打出前目标再次变化、战斗刚开始第一轮出生事件。
- 复查 `Stage01SampleContentBuilder.cs` 和生成 `.asset` 的数值语义，特别是 `AttackPowerModifier` 使用小数百分比、`Stun` 持续时间、主动后撤距离、大招路径伤害倍率。

### 验证情况
- 已执行 Unity batchmode：`Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentBatch`，日志包含 `Tundra build success (28.00 seconds)` 和 `Cleanup mono`，炽焰咆哮虎英雄与技能 `.asset` 已生成。
- 已执行 Unity batchmode 编译检查，日志包含 `Tundra build success (31.61 seconds)`，未发现 `error CS` 或 `Stage-01 demo content validation failed`。
- 编译检查没有干净退出标记；Unity 进程在 reload/退出阶段长时间无进展后被强制停止，因此不能描述为完整 clean quit。
- 已执行 `git diff --cached --check`，提交前通过；新增 Unity YAML 里的尾随空格已清理。
- 已抽查关键生成资产：首击 `sameTargetCooldownSeconds: 2.5`、出生 / 复活 `triggerStatusEffectsOnInitialSpawn: 1` 与 `triggerStatusEffectsOnRevive: 1`、群体减攻 `magnitude: -0.18`、主动减攻 `magnitude: -0.3`、大招路径伤害 `powerMultiplier: 3`。
- 未做完整手动观战回放、自动战斗用例或专门平衡回归测试；评审 AI 需要补查实战表现和边界行为。

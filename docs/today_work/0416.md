# 2026-04-16 Today Work

## 当前定稿：投射物普攻英雄的受压后撤

这份文档现在只保留 2026-04-16 最终收口后的方案，不再保留当天早些时候已经过时的草案、审查交接和中间判断。

## 当前方案

- 这是一条 `通用 AI 行为规则`，不是状态效果，也不是某个英雄特例。
- 只有 `普攻配置 usesProjectile = true` 的英雄，才默认接入这条后撤逻辑。
- 这条规则包含射手、法师，也包含使用投射物普攻的辅助。
- 行为目标是“被危险 threat 压近时短暂拉开”，不是做完整仇恨系统，也不是做完整走 A 状态机。

## 当前触发语义

- 英雄最近短时间内被敌方英雄造成了 `有效的直接伤害`，才会记录 retreat threat。
- `DOT / 持续伤害` 不再刷新这条 threat。
- 只有本帧没有先因为技能释放退出流程、且暂时不能立刻打出下一次普攻时，才允许进入后撤判断。
- threat 不能只满足“我刚掉血了”，还要满足“这个 threat 真的危险到值得后撤”。

## 当前威胁收口

- 近战、短手、已经贴近的敌人：可以正常触发后撤。
- 远程 threat：只有当它已经压进异常近距离时，才允许触发后撤。
- 正常的 `远程对远程互点`，不应仅仅因为双方刚互相打到就双向后撤。

## 当前执行位置

- `RuntimeHero` 保存最近 threat 的短时运行时记忆。
- `BattleAiDirector` 负责判断这个 threat 是否足够危险、当前是否应该后撤。
- `BattleSimulationSystem` 在“技能尝试之后、默认前压 / 普攻之前”插入 retreat 分支。

## 当前明确边界

- 不把这套能力做成 `StatusEffect`。
- 不扩成完整仇恨系统。
- 不为每个远程英雄单独写一套拉扯特例。
- 不把单位做成长期逃跑状态。
- threat 窗口结束后，单位应恢复原本的索敌、前压、普攻和技能流程。

## 本次代码落地对应

实现提交分两步：

- `35466df` `Add threat-based ranged retreat AI`
- `1f71d58` `Refine ranged retreat trigger rules`

当前应以第二次收口后的行为为准，也就是：

- `DOT` 不算 retreat threat
- 辅助不排除
- 正常远程互点不应机械式双后撤

## 已同步的正式文档

本次已经把这条规则同步进正式规划文档：

- `docs/planning/stage-01-combat-rules-decisions.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `docs/planning/stage-01-architecture-decisions.md`
- `docs/planning/stage-01-first-implementation-plan.md`
- `docs/planning/stage-01-status-effect-decisions.md`

## 仍需进游戏观察的点

详细观察项继续看：

- `docs/today_work/0416_user_check.md`

## 补充交接：击飞 / 定向位移实现

这部分是 2026-04-16 当天后续新增的另一组实现，不属于上面的“投射物普攻英雄受压后撤”规则，但需要一起交接给后续检视 AI。

### 这次我实际做了什么

- 目标不是把 `击飞` 继续当成 `眩晕` 复用，而是把它收成：
  - `KnockUp` 负责硬控语义
  - 独立的 `ApplyForcedMovement` 负责可选的水平位移和空中抬升表现
- 我新增了 `SkillType.KnockUp`。
- 我新增了 `SkillEffectType.ApplyForcedMovement`。
- 我在 `SkillEffectData` 里补了强制位移所需的最小字段：
  - `forcedMovementDirection`
  - `forcedMovementDistance`
  - `forcedMovementDurationSeconds`
  - `forcedMovementPeakHeight`
- 我新增了 `game/Assets/Scripts/Heroes/RuntimeForcedMovement.cs`，作为英雄运行时的独立强制位移过程。
- 我在 `RuntimeHero` 里补了：
  - `IsUnderForcedMovement`
  - `VisualHeightOffset`
  - 强制位移 tick / 死亡清理 / 复位清理
- 我在 `BattleSimulationSystem` 里让单位在强制位移期间不继续执行自发移动、普攻和施法决策。
- 我在 `BattleSkillSystem` 里接入了 `ApplyForcedMovement` 的执行逻辑，并新增 `ForcedMovementAppliedEvent`。
- 我在 `BattleView` 里接了 `VisualHeightOffset`，让“击飞到空中”至少有最小可见表现。
- 我在 `BattleDebugHud` 里补了强制位移日志。
- 我把坦克样例技能从原本的眩晕型技能，改成了击飞 + 强制位移组合：
  - `Shield Bash`
  - `Ground Lock`
- 我同步补了正式规则文档：
  - `docs/planning/stage-01-status-effect-decisions.md`

### 这次实现对应提交

- `fd9774f` `Add knock up forced movement support`
- `70161a9` `Update tank demo skills to use knock up`

### 按 0416check 收口后的处理

基于 `docs/today_work/0416check.md` 的复核，本轮又额外做了两类收口：

- 已修 1：把 `Stage01SampleContentBuilder` 里“首次创建资产但 `overwriteExistingContent = false` 时直接返回”的断链风险统一修掉。
- 已修 2：把强制位移期间的攻击 / 施法门禁，也收回 `RuntimeHero` 的统一查询入口。

#### 已修 1：样例内容生成器的首次创建路径

这次实际修的不是单独一个 `KnockUp` case，而是一类共同问题：

- 之前多个包装函数都会先调用 `CreateSkill(...)`
- 然后如果 `overwriteExistingContent = false` 就直接 `return skill`
- 这会导致“新资产首次创建时”保留默认半成品，而不是进入后面的定制步骤

本轮已收口的点：

- `CreateSkill(...)` 现在会额外返回“该资产在调用前是否已存在”的信息
- 所有需要“只保护旧资产、不影响新资产”的包装函数，现在都改成：
  - 旧资产 + 不覆盖：直接保留
  - 新资产首次创建：继续完成完整初始化
- 这次一并收口的入口包括：
  - `CreateMageActiveBurstSkill(...)`
  - `CreateSupportActiveSkill(...)`
  - `CreateSupportUltimateSkill(...)`
  - `CreateStunSkill(...)`
  - `CreateKnockUpSkill(...)`
  - `CreateBuffSkill(...)`
  - 各职业 `Configure...Ultimate(...)`
  - `ConfigureSupportBasicAttack(...)`

这意味着：

- 现在在干净工程或首次生成 demo content 时
- 自定义技能和支持型普攻不会再因为“非覆盖模式”被生成为半成品或错误默认值

#### 已修 2：强制位移门禁一致性

`0416check` 里提到的结构性风险，本轮也顺手收口了：

- `RuntimeHero.CanAttack`
- `RuntimeHero.CanCastSkills`

现在都已经把 `IsUnderForcedMovement` 一并算进去了。

这意味着：

- 当前不仅 `BattleSimulationSystem` 顶层会在强制位移期间直接短路
- 就连运行时行为查询本身，也和“强制位移期间不应攻击 / 不应施法”的语义保持一致

### 本轮明确没有改的点

下面这些点，这轮确认后保留原样，没有继续改：

- `StatusEffectCatalog` 里 `Stun` 和 `KnockUp` 继续共用同一组硬控 flag
  - 原因：两者当前的区别本来就不靠 flag 表区分
  - 当前差异语义来自：
    - `KnockUp` 这个独立状态类型
    - `ApplyForcedMovement`
    - `VisualHeightOffset`
- `Shield Bash` / `Ground Lock` 的位移与抬升参数这轮没有再调
  - 原因：`0416check` 的判断是“当前参数没有明显越界，更需要实机观察而不是先盲调”
- 没有为了“看起来不同”去强行把 `KnockUp` 改成另一套独立行为 flag
  - 原因：当前正式文档要求的是“硬控 + 独立位移效果组合”
  - 不是要求 `KnockUp` 在行为门禁层就必须和 `Stun` 拥有完全不同的一组阻断位

### 本轮补做的验证

- 已使用本机 `Unity 6000.3.13f1` 脱离沙箱执行一次 batchmode 编译验证。
- 当前结果是：
  - `Assembly-CSharp.dll`
  - `Assembly-CSharp-Editor.dll`
  - 均已成功完成脚本编译
  - 日志里有明确的 `Tundra build success`
- 之前那条 `BasicAttackEffectType could not be found` 的批处理报错，本轮没有复现。
- 现在回看，更像是之前沙箱环境下：
  - 本地用户目录权限不足
  - Unity Package Manager IPC 连接失败
  - 导致没有真正进入完整编译阶段
- 因此这条旧错误记录现在应视为过期，不再作为本轮未解决问题保留。
- 当前仍能看到的只是一些已有的过时 API warning，主要在：
  - `Assets/FantasyWorkshop/Common/Scripts/Utils/TextureImporter.cs`
  - `Assets/HeroEditor4D/Common/Scripts/EditorScripts/...`
  - `Assets/HeroEditor4D/Common/Scripts/Editor/...`

### 这次涉及的主要文件

- `game/Assets/Scripts/Data/SkillType.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Heroes/RuntimeForcedMovement.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/UI/BattleView.cs`
- `game/Assets/Scripts/UI/BattleDebugHud.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Skills/tank_001_ironwall/Shield Bash.asset`
- `game/Assets/Data/Stage01Demo/Skills/tank_001_ironwall/Ground Lock.asset`
- `docs/planning/stage-01-status-effect-decisions.md`

## 给检视 AI 的明确检查项

如果另一个 AI 要检视这轮“击飞 / 定向位移”改动，重点不要泛泛而谈，而要直接检查下面这些点：

### 1. 语义是否真的和眩晕分开

- `KnockUp` 是否只是复用了 `Stun` 的行为门禁，还是已经形成“硬控 + 可选位移”的组合语义。
- 当前实现是否符合正式文档约束：
  - `击飞` 本身负责控制
  - 推开 / 拉近属于独立位移效果

### 2. 运行时边界是否合理

- 强制位移期间，单位停止自发移动 / 普攻 / 技能决策，这个边界是否合理。
- 强制位移结束后，是否能自然回到原本 AI 流程。
- 死亡、复活、ResetToSpawn 时，强制位移状态是否都被正确清理。

### 3. 表现层是否保持最小且安全

- `VisualHeightOffset` 只影响表现，不应该偷偷改战斗判定。
- 当前“空中效果”是否只是视觉抬升，而不是引入未被规则约束的 3D 物理含义。

### 4. 数据契约和旧资产兼容性

- `SkillEffectType` / `SkillType` 新增枚举值是否安全。
- `SkillEffectData` 新字段的默认值是否会破坏旧技能资产。
- 样例坦克技能 YAML 直接改成 `KnockUp + ApplyForcedMovement` 后，是否与当前运行时读取路径一致。

### 5. 坦克样例技能参数是否合适

- `Shield Bash` 的击退距离、持续时间、抬升高度是否偏大或偏小。
- `Ground Lock` 作为范围控制大招，当前击飞 / 位移强度是否合理，是否会造成过强的群体打断。

### 6. 是否有遗漏的引用或断链

- 新增 `ApplyForcedMovement` 后，是否还有未覆盖的 `switch` / 条件分支。
- 是否还有地方仍然只按 `Stun` 写死判断，导致 `KnockUp` 实际没完全接入。

### 7. 当前验证结论要单独看待

- 我已经尝试用本机 Unity：
  - `C:\Program Files\Unity 6000.3.13f1\Editor`
  做批处理验证。
- 这次在脱离沙箱后，已经拿到了“工程当前可完成脚本编译”的结论。
- 日志里明确出现了：
  - `Tundra build success`
  - `Assembly-CSharp.dll`
  - `Assembly-CSharp-Editor.dll`
  - 编译完成
- 因此之前那条 `BasicAttackEffectType` 批处理错误，不再应被视为当前有效结论。
- 检视 AI 现在更应该关注：
  - 运行时语义是否正确
  - 样例技能参数是否合理
  - 是否还有未覆盖的逻辑分支

### 8. 检视输出建议

希望另一个 AI 的输出至少明确回答：

- 这轮实现里最值得保留的点是什么
- 最可能有风险的点是什么
- 有没有与正式文档冲突的地方
- 如果要继续扩“带方向击飞”，下一步最小增量应该落在哪个模块

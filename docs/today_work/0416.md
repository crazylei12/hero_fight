# 2026-04-16 Review Handoff

## 文档用途

这份文档是给“另一个 AI 审查本次 2026-04-16 改动”的对照清单。

请审查 AI 优先按本文件列出的范围检查，不要把当前工作区里其他无关脏改动一起算进本轮 review。

本轮对应提交：
- `c226092`
- commit message: `Implement Sunpriest healing support kit`

## 这轮改了什么

本轮目标是把原先占位的辅助英雄真正落成第一版可运行模板，并顺手把两个底层能力通用化：

- 把 `support_001_sunpriest` 从旧版占位设计改成“治疗普攻 + 单体治疗护盾 + 持续范围治疗大招”
- 把“普攻打友军并治疗”做成通用基础攻击能力，不只给圣职者写特例
- 把 `Shield` 纳入统一状态系统
- 把持续区域机制收束成“创建持续区域 + 区域脉冲载荷”，避免写成 `PersistentAreaHeal` / `PersistentAreaBuff` 这类越来越碎的组合枚举
- 同步更新了规划文档和英雄文档，避免实现和规则脱节

## 审查范围

请只审查以下文件。

文档：
- `docs/planning/stage-01-combat-rules-decisions.md`
- `docs/planning/stage-01-hero-spec-decisions.md`
- `docs/planning/stage-01-status-effect-decisions.md`
- `docs/heroes/support/sunpriest.md`

运行时脚本：
- `game/Assets/Scripts/Data/BasicAttackData.cs`
- `game/Assets/Scripts/Data/SkillType.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Data/StatusEffectType.cs`
- `game/Assets/Scripts/Heroes/RuntimeStatusEffect.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Battle/RuntimeBasicAttackProjectile.cs`
- `game/Assets/Scripts/Battle/BattleAiDirector.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/UI/BattleDebugHud.cs`

编辑器生成器：
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`

现有示例资产：
- `game/Assets/Data/Stage01Demo/Heroes/support_001_sunpriest/Sunpriest.asset`
- `game/Assets/Data/Stage01Demo/Skills/support_001_sunpriest/Radiant Heal.asset`
- `game/Assets/Data/Stage01Demo/Skills/support_001_sunpriest/Sun Blessing.asset`

## 预期实现语义

### 1. 圣职者

预期语义如下：

- 英文名和项目代号固定为 `Sunpriest`
- 英雄 ID 固定为 `support_001_sunpriest`
- 普攻为远程投射物治疗普攻
- 普攻目标为友方，优先最低血量友军，允许包含自己
- 小技能 `Radiant Heal` 为单体直接治疗 + 单体护盾
- 大招 `Sun Blessing` 为持续区域治疗，不跟随施法者移动

### 2. 普攻通用化

预期不是“给 Sunpriest 单独写一套特殊普攻逻辑”，而是：

- `BasicAttackData` 允许描述普攻目标类型
- `BasicAttackData` 允许描述普攻效果类型
- `BattleSimulationSystem` 能按普攻目标类型重新选目标
- `BattleBasicAttackSystem` 能按普攻效果类型走伤害或治疗结算
- 投射物普攻仍走统一 projectile 流程

### 3. 护盾

预期不是技能脚本临时维护一份额外护盾值，而是：

- `Shield` 成为正式 `StatusEffectType`
- 伤害统一从 `RuntimeHero.ApplyDamage` 入口先扣护盾再扣血
- 护盾值存在运行时状态里，而不是 HeroDefinition 或技能特例字段里

### 4. 持续区域

这次按用户补充意见改成了更通用的方向。

预期不是：
- `PersistentAreaDamage`
- `PersistentAreaHeal`
- `PersistentAreaBuff`

而是：
- `SkillEffectType.CreatePersistentArea`
- 区域自身有目标阵营配置
- 区域自身有脉冲效果类型配置
- 区域脉冲时再决定是直接伤害、直接治疗，还是只施加状态

## 建议审查顺序

### 第一组：数据契约是否合理

优先看：
- `BasicAttackData.cs`
- `SkillType.cs`
- `SkillEffectType.cs`
- `SkillEffectData.cs`
- `StatusEffectType.cs`

重点检查：
- 新增枚举值是否保持对旧资产的兼容性
- `SkillEffectType.CreatePersistentArea` 是否真的比新增 `PersistentAreaHeal` 更通用
- `BasicAttackData.damageMultiplier` 现在也被治疗普攻复用，这个命名是否还能接受，还是应该继续抽象成更中性的字段
- `SkillType.AreaHeal` 是否只承担“语义和目标策略”职责，而没有把区域治疗写成另一套独立运行时

### 第二组：运行时逻辑是否真的复用统一系统

优先看：
- `RuntimeHero.cs`
- `RuntimeStatusEffect.cs`
- `BattleSimulationSystem.cs`
- `BattleBasicAttackSystem.cs`
- `BattleSkillSystem.cs`
- `BattleAiDirector.cs`

重点检查：
- 友方普攻是否真的走统一基础攻击系统，而不是写成隐藏技能
- 护盾是否真的只在统一受伤入口处理
- 持续区域是否真的走统一 `RuntimeSkillArea`，而不是又分出一套治疗区域运行时
- `AreaHeal` 是否只影响目标选择/机会判断，而不破坏现有技能系统结构

### 第三组：生成器和资产是否一致

优先看：
- `Stage01SampleContentBuilder.cs`
- `Sunpriest.asset`
- `Radiant Heal.asset`
- `Sun Blessing.asset`
- `docs/heroes/support/sunpriest.md`

重点检查：
- 生成器里的默认值是否和现有资产、英雄文档一致
- 后续重新生成 demo content 时，是否会保留这次的圣职者设计，而不是回退到旧版占位配置
- 文档描述与实际参数是否冲突

## 需要重点怀疑的点

下面这些点是本轮最值得怀疑、最值得对着代码找 bug 的地方。

### A. 友方普攻的目标刷新节奏

相关文件：
- `BattleSimulationSystem.cs`
- `BattleAiDirector.cs`

这次为了让治疗普攻优先追最低血量友军，我让“友方普攻目标”更积极地重选目标。

请重点检查：
- 会不会导致目标频繁抖动
- 会不会产生过多 `TargetChangedEvent`
- 会不会出现治疗者在多个友军间来回切换，反而很难出手
- 当全队满血时，当前的“健康友军兜底站位锚点”逻辑是否合理

### B. 自疗与不可选中的边界

相关文件：
- `BattleBasicAttackSystem.cs`
- `BattleSimulationSystem.cs`

当前实现里，友方目标如果是自己，会放宽一层直接目标合法性判断。

请重点检查：
- 这是否会让 `Untargetable` 下的自疗/自护盾产生不符合设计的例外
- 这种例外是否应该只允许技能自施法，不应该允许普攻自疗

### C. 护盾耗尽时机和事件时机

相关文件：
- `RuntimeHero.cs`
- `RuntimeStatusEffect.cs`
- `BattleSimulationSystem.cs`

当前护盾是在 `ApplyDamage` 中被消耗，状态本体是否移除则依赖后续 tick。

请重点检查：
- 护盾刚被打空时是否应该立刻移除，而不是等下一帧 tick
- `StatusRemovedEvent` 的抛出时机是否会因此延后
- 多层护盾叠加时，当前按列表顺序消耗是否合理

### D. 持续区域组合是否真能承接后续 Buff / Debuff

相关文件：
- `SkillEffectData.cs`
- `BattleSkillSystem.cs`

这次的区域通用化目前支持：
- 区域目标阵营
- 区域脉冲直接伤害
- 区域脉冲直接治疗
- 区域脉冲只施加状态
- 直接伤害/直接治疗时可附带状态

请重点检查：
- 这套设计是否足以支撑后续范围 Buff / 范围 Debuff
- 有没有仍然残留“范围治疗是特例、范围伤害是另一套”的结构性问题
- `PersistentAreaPulseEffectType.None + statusEffects` 这个设计是否清晰，还是会在后续变得难懂

### E. 旧逻辑回归风险

相关文件：
- `BattleBasicAttackSystem.cs`
- `BattleSimulationSystem.cs`
- `BattleSkillSystem.cs`

请重点检查：
- 普通打敌人的普攻有没有被这次改坏
- 火法 `Ember Burst` 和 `Meteor Fall` 的区域伤害是否仍保持旧语义
- 旧有单体治疗技能有没有被 `AreaHeal` 或新目标逻辑误伤

### F. 生成器与现有资产的双轨一致性

相关文件：
- `Stage01SampleContentBuilder.cs`
- `Sunpriest.asset`
- `Radiant Heal.asset`
- `Sun Blessing.asset`

请重点检查：
- 生成器重新跑一次时，是否会生成和当前 YAML 资产一致的结构
- 新增字段在 YAML 里是否都补齐了
- 不重建资源的情况下，当前已有资产能否被运行时正确消费

## 这轮没做的验证

这部分请审查 AI 特别注意，因为它们是本轮明确存在的验证缺口。

- 我没有在 Unity Editor 里实际跑一场战斗确认 Console
- 我没有确认 `Sunpriest` 的治疗投射物视觉是否满意
- 我没有做 Battle 场景人工观察
- 当前终端环境里没有找到可直接用的 `.sln` / `.csproj`，所以没做编译级验证

因此请另一个 AI 如果有条件，优先补以下验证判断：

- 代码是否至少语义上能编译
- `BattleView` 对没有专属 projectile prefab 的治疗投射物是否仍有合理 fallback 表现
- `Sunpriest` 的普攻、小技能、大招是否都还能进统一日志

## 审查时的对照结论标准

如果你是另一个 AI，请优先回答下面这些问题：

1. 这次是否真的把“友方普攻”做成了通用基础能力，而不是只为圣职者加特例？
2. 护盾是否真的进入了统一状态系统，而不是技能局部变量？
3. 持续区域的抽象是否已经朝“区域 + 脉冲效果载荷”的方向收束？
4. `Stage01SampleContentBuilder` 和现有资产是否一致？
5. 是否存在会破坏旧英雄、旧普攻或旧区域伤害技能的回归风险？
6. 代码里最值得修的 bug 或结构问题是什么？

## 明确排除项

以下内容不属于本轮 review 目标：

- 工作区中 `HeroEditor4D` 那批大量脏改动
- `DOTween` 目录下的删除/变更
- 与 `c226092` 无关的其他历史改动

如果审查报告把这些无关脏改动也算进来，会污染本轮结论。

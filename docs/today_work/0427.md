## [13:16] 本轮检查文档

### 本次做了什么
- 设计并落地了新的辅助英雄 `Commander / 指挥官`，定位是“稳定提升队友输出”的远程辅助。
- 新增英雄文档：`docs/heroes/support/commander.md`，明确普攻、小技能 `Battle Orders`、大招 `Focus Fire Command`、数值和落地边界。
- 实现 Commander 小技能：选择当前累计造成伤害最高的友军，施加 `AttackPowerModifier +25%`，持续 `5 秒`，冷却 `7 秒`。
- 实现 Commander 大招：创建持续 `6 秒` 的团队集火指令，对目标施加 `FocusFireMark` 与 `DefenseModifier -35%`；目标死亡、复活等待、不可选中或失效时，在剩余持续时间内重新选择下一个高输出敌方目标。
- 新增集火指令运行时系统，支持每队一个当前集火目标、状态施加/移除、过期、替换、目标迁移和日志事件。
- 调整普攻目标选择：敌方伤害普攻在嘲讽之后优先响应当前团队集火标记，再进入同目标保持或普通索敌；治疗、护盾、增益类目标不受集火标记影响。
- 更新规划文档中关于 `FocusFireMark / 集火标记` 的状态语义、战斗规则和日志要求。
- 生成并提交 Commander 的 Unity 数据资产；同时提交生成器已引用的 TrollWarlord 数据资产，避免 `Stage01HeroCatalog.asset` 出现断引用。
- 本轮代码与资产已提交：`5509b9a game: add commander focus fire support`。

### 涉及文件
- `docs/heroes/support/commander.md`
- `docs/planning/stage-01-combat-rules-decisions.md`
- `docs/planning/stage-01-status-effect-decisions.md`
- `game/Assets/Scripts/Battle/BattleFocusFireCommandSystem.cs`
- `game/Assets/Scripts/Battle/RuntimeFocusFireCommand.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/BattleContext.cs`
- `game/Assets/Scripts/Battle/BattleEvents.cs`
- `game/Assets/Scripts/Battle/BattleLogSession.cs`
- `game/Assets/Scripts/Battle/BattleSimulationSystem.cs`
- `game/Assets/Scripts/Data/StatusEffectType.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillType.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Heroes/support_006_commander/`
- `game/Assets/Data/Stage01Demo/Skills/support_006_commander/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`
- `game/Assets/Data/Stage01Demo/Heroes/warrior_005_trollwarlord/`
- `game/Assets/Data/Stage01Demo/Skills/warrior_005_trollwarlord/`

### 需要检查什么
- 检查 `FocusFireMark` 是否只影响敌方伤害普攻目标选择，不应改写治疗、护盾、增益、保护类技能目标。
- 检查集火优先级是否符合预期：嘲讽优先级最高，其次是集火标记，然后才是同目标普攻保持和普通索敌。
- 检查 `BattleFocusFireCommandSystem` 的目标迁移逻辑：目标死亡、不可选中、复活等待、失效、指令过期、同队新指令替换时，状态移除和日志是否一致。
- 检查 `FocusFireMark` 与 `DefenseModifier` 是否共享剩余持续时间，迁移时是否不会刷新大招总持续时间。
- 检查 Commander 的运行时数值是否与英雄文档一致：普攻倍率 `0.9`、攻击距离 `8.5`、小技能 `+25% / 5s / 7s CD`、大招 `6s / -35% defense / 11 range`。
- 检查 `Stage01HeroCatalog.asset` 中 Commander 和 TrollWarlord 引用的 GUID 是否都能在对应资产中解析。
- 当前工作区还存在未提交改动：`docs/planning/stage-01-hero-spec-decisions.md`，内容属于 TrollWarlord 的同目标普攻叠层规划，不属于本轮 Commander 提交；评审时不要误认为它已包含在 `5509b9a`。

### 给评审 AI 的重点
- 优先审查 `game/Assets/Scripts/Battle/BattleFocusFireCommandSystem.cs`：确认指令注册、旧指令替换、状态施加/移除、目标迁移、过期清理都不会留下残留状态或空引用。
- 重点看 `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`：确认集火目标选择不会绕过 `IsValidTarget`、`CanExecuteAgainstTarget`、射程、死亡、不可选中、嘲讽等既有门禁。
- 检查 `game/Assets/Scripts/Battle/BattleSkillSystem.cs` 对 `CreateFocusFireCommand` 的接入是否只对直接单位效果生效，没有影响区域技能、延迟技能或其他技能效果。
- 检查 `StatusEffectType` 新增 `FocusFireMark = 20`、`DeathPrevent = 21` 是否与现有资产序列化兼容；尤其注意旧资产中已序列化的状态枚举值是否被意外改义。
- 检查 `Stage01SampleContentBuilder` 中 Commander 资产生成逻辑、技能 owner override、英雄目录注册是否与现有 demo 内容生成模式一致。
- 对照 `docs/planning/stage-01-combat-rules-decisions.md`、`docs/planning/stage-01-status-effect-decisions.md` 和 `docs/heroes/support/commander.md`，确认文档语义没有把 `FocusFireMark` 写成 `Taunt` 或完整仇恨系统。
- 由于本轮涉及 Unity 工程代码和资产，应同时按根目录 `AGENTS.md` 与 `game/AGENTS.md` 的实现约束复查。

### 验证情况
- 已执行 Unity 编译：`C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` batchmode，日志 `game/Logs/codex-commander-compile-3.log`，退出码 `0`。
- 已执行 demo 内容生成；生成过程最初的 shell 等待超时，但 Unity 进程随后正常退出，并生成 Commander 及 TrollWarlord 相关资产。
- 已执行离线 smoke：蓝方包含 `support_006_commander`，红方包含 `warrior_005_trollwarlord`，日志 `game/Logs/codex-commander-smoke-3.log`，退出码 `0`。
- smoke 战斗日志确认：Commander 在 `14.5s` 对 Frostmage 施加 `FocusFireMark` 和 `DefenseModifier -0.35`；队友随后转火 Frostmage；`20.4s/20.5s` 两个状态与集火指令正常过期。
- 已执行 `git diff --cached --check`，提交前暂存内容无空白错误。
- 本轮未新增专门的自动化单元测试；后续建议补针对集火迁移、过期清理、嘲讽优先级、治疗目标不受影响的更小粒度测试。

## [13:19] 巨魔战将实现交接

### 本次做了什么
- 设计并实现新的战士英雄 `TrollWarlord / 巨魔战将`，定位是近战单挑压制、对敌方前排有效，但团战泛用性受目标切换限制。
- 新增英雄文档：`docs/heroes/warrior/trollwarlord.md`。
- 按用户确认调整关键参数：基础攻击距离 `2.0`；`Fervor` 改为 `6` 层、每层 `+16%` 攻速、满层仍为 `+96%`。
- 新增共享普攻能力 `BasicAttackSameTargetStackData`，用于配置同目标普攻叠层、层数上限、每层收益、目标保持范围，以及大招期间满层覆写状态。
- 在运行时接入同目标普攻叠层：直接伤害普攻命中同一目标后叠层；切换目标、死亡、复活会清层；攻速计算走 `RuntimeHero.AttackInterval` 的统一入口。
- 新增 `DeathPrevent` 状态语义：不阻止伤害、不等同无敌，只在最终生命损失后把生命值钳制到配置下限。
- 在 `Stage01SampleContentBuilder` 中接入 `warrior_005_trollwarlord`、小技能 `Warlord Reach` 和大招 `Deathless Frenzy`。
- 本轮巨魔战将代码与文档提交：`31b1609 heroes: add troll warlord prototype`。
- 当前仓库后续提交 `5509b9a game: add commander focus fire support` 已生成并提交 TrollWarlord 的 Unity 数据资产；这些资产不在 `31b1609` 中，但当前工作树可读到。

### 涉及文件
- `docs/heroes/warrior/trollwarlord.md`
- `game/Assets/Scripts/Data/BasicAttackData.cs`
- `game/Assets/Scripts/Data/StatusEffectType.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Heroes/StatusEffectSystem.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/ResolvedBasicAttack.cs`
- `game/Assets/Scripts/Battle/RuntimeBasicAttackProjectile.cs`
- `game/Assets/Scripts/Battle/BattleStatsSystem.cs`
- `game/Assets/Scripts/Editor/BalanceSheetService.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Heroes/warrior_005_trollwarlord/`
- `game/Assets/Data/Stage01Demo/Skills/warrior_005_trollwarlord/`

### 需要检查什么
- 检查 `Fervor` 叠层速度是否符合 60 秒战斗节奏：当前为 `6` 次命中叠满，基础攻速约 `1.20 秒/次`，实战满层时间应明显短于旧的 `8` 层方案。
- 检查同目标保持逻辑是否不会越权：嘲讽、死亡、不可选中、强制目标、目标无效仍应优先清层或切换。
- 检查 `DeathPrevent` 是否只钳制普通伤害后的生命值，不应阻止护盾消耗、伤害日志、控制、选中，也不应变成 `Invulnerable`。
- 检查大招期间 `fullStackOverrideStatusEffectType = DeathPrevent` 是否只把同目标攻速收益视为满层，不应额外叠加另一份 `AttackSpeedModifier +96%`。
- 检查资产中 `StatusEffectType` 枚举值：当前 TrollWarlord 资产里 `fullStackOverrideStatusEffectType` 随后续 Commander 提交变成 `21`，因为 `FocusFireMark = 20`、`DeathPrevent = 21`；需确认没有旧资产误解枚举值。
- 当前工作区仍有未提交 `docs/planning/stage-01-hero-spec-decisions.md`，内容是同目标普攻叠层规划补充；评审时确认它是否应单独提交或并入后续整理。

### 给评审 AI 的重点
- 优先审查 `RuntimeHero`：确认同目标叠层只保存在运行时，不回写配置；目标切换、死亡、复活都能清层；攻速加成与被动攻速、状态攻速的乘加关系符合预期。
- 重点审查 `BattleBasicAttackSystem`：确认只在英雄本体的直接伤害普攻命中时叠层，投射物能携带配置，弹射和 deployable proxy 不会错误叠 TrollWarlord 的层数。
- 检查 `StatusEffectSystem.GetDeathPreventHealthFloor` 与 `RuntimeHero.ApplyHealthLoss`：确认低于 1 血时不会被死亡保护“抬血”，正常伤害仍会记录实际生命损失。
- 检查 `Stage01SampleContentBuilder`：确认 `Warlord Reach` 是当前目标在 `2.8` 内才释放的自身增益；`Deathless Frenzy` 是当前目标存在且低血触发的一局一次大招。
- 对照 `docs/heroes/warrior/trollwarlord.md`，确认实现与文档一致，尤其是 `2.0` 攻击距离、`6` 层、每层 `+16%`、大招不免疫未来斩杀。
- 因本轮涉及 `game/` 实现，应按 `game/AGENTS.md` 重点复查统一状态系统、统一普攻配置和最小战斗规则是否仍未被英雄特例破坏。

### 验证情况
- 已尝试使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `g:\BaiduNetdiskDownload\fight\game` 执行 batchmode demo 内容生成。
- 该次 Unity 批处理被阻塞，原因是同一个 Unity 项目已在另一个编辑器实例中打开；日志：`game/Logs/codex_trollwarlord_generate.log`，退出码 `1`。
- 随后检查当前 Unity `Editor.log`，未发现 `error CS`、`Compilation failed` 或 `Scripts have compiler errors`，并看到 assembly reload 成功记录；这不是完整替代编译验证。
- 本轮没有单独完成 TrollWarlord 的战斗 smoke 测试；后续 Commander 交接记录中已有包含 TrollWarlord 资产生成和一次含 TrollWarlord 的 smoke，但评审时仍建议单独验证其叠层与大招行为。
- 已执行提交：`31b1609 heroes: add troll warlord prototype`。

## [14:21] Demon 刺客实现交接

### 本次做了什么
- 根据新英雄文档实现刺客英雄 `Demon / 恶魔`，并提交：`9f7e8ac Add Demon assassin hero`。
- 新增共享目标类型 `FarthestEnemyFromSelf`，用于选择“当前距离自己最远的敌方单位”，并支持 `minimumTargetDistance` 门槛。
- 新增技能效果 `SwapPositionsWithPrimaryTarget`，Demon 小技能 `Infernal Exchange` 会与最远敌方交换位置，不造成伤害和硬控。
- 新增技能效果 `ApplyCombatFormOverride` 与运行时形态覆写：可覆盖形态 `formKey`、是否远程、弹速、攻击距离、攻击力和攻速；Demon 大招 `Greater Demon Form` 变身持续到死亡结束。
- 调整普攻和 AI：远程/近战判断改读 `RuntimeHero.UsesProjectileBasicAttack`，变身后 Demon 会按远程英雄使用拉扯逻辑。
- 调整表现层：`HeroVisualConfig` 新增 `formVisuals`，`BattleView` 可按 `CurrentVisualFormKey` 重建模型；Demon 已预留 `greater_demon` 形态模型槽位，当前用 Butcher prefab 和 Longshot projectile 作为占位。
- 新增 Demon 的 Stage01 demo hero/skill asset，并把 `Stage01HeroCatalog.asset` 加入 Demon。

### 涉及文件
- `docs/heroes/assassin/demon.md`
- `game/Assets/Scripts/Data/SkillTargetType.cs`
- `game/Assets/Scripts/Data/SkillEffectType.cs`
- `game/Assets/Scripts/Data/SkillData.cs`
- `game/Assets/Scripts/Data/SkillEffectData.cs`
- `game/Assets/Scripts/Data/HeroVisualConfig.cs`
- `game/Assets/Scripts/Heroes/RuntimeHero.cs`
- `game/Assets/Scripts/Battle/BattleAiDirector.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Battle/BattleBasicAttackSystem.cs`
- `game/Assets/Scripts/Battle/ResolvedBasicAttack.cs`
- `game/Assets/Scripts/Battle/RuntimeBasicAttackProjectile.cs`
- `game/Assets/Scripts/Battle/RuntimeBasicAttackBounceChain.cs`
- `game/Assets/Scripts/UI/BattleView.cs`
- `game/Assets/Scripts/UI/GenericAnimatorBattleAnimationDriver.cs`
- `game/Assets/Scripts/UI/HeroEditor4DBattleAnimationDriver.cs`
- `game/Assets/Scripts/UI/SpriteSheetBattleAnimationDriver.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Heroes/assassin_005_demon/`
- `game/Assets/Data/Stage01Demo/Skills/assassin_005_demon/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 检查 `FarthestEnemyFromSelf` 是否只选择敌方、非死亡、可直接选中的单位，并且 `minimumTargetDistance` 不会误挡全图交换。
- 检查 `SwapPositionsWithPrimaryTarget` 是否正确保存双方起点后再移动，避免先移动 caster 后导致 target 被送到错误位置。
- 检查位置互换是否应该受碰撞/落点冲突处理约束；当前实现直接交换双方原位置并走 `StartForcedMovement`。
- 检查 `ApplyCombatFormOverride` 是否符合“大招持续到死亡结束”：当前 `durationSeconds = 0` 表示无限期，`expiresOnDeath = true` 表示死亡清除。
- 检查变身后远程普攻是否完整走 `UsesProjectileBasicAttack`、`CurrentBasicAttackProjectileSpeed` 和 `CurrentVisualFormKey`，尤其是弹道创建、弹射链和动画选择。
- 检查 `BattleView.RebuildHeroVisual` 重建模型时是否会误清状态特效、临时特效、血条或排序；当前模型根节点与 `StatusVfx` 分离。
- 检查手工补齐的 `.asset` YAML 是否与 `Stage01SampleContentBuilder` 默认值一致，特别是 Demon 大招的 `formOverride`、`greater_demon` 形态 prefab/projectile 引用和 catalog GUID。

### 给评审 AI 的重点
- 优先审查 `RuntimeHero` 的形态覆写：攻击距离、攻击力、攻速、远程状态、弹速、死亡清除和复活重置是否都走统一入口。
- 重点看 `BattleBasicAttackSystem`：确认普攻 projectile 和 bounce chain 都快照了 `VisualFormKey`，避免变身结束或死亡后已发射弹道显示错形态。
- 审查 `BattleAiDirector`：确认 Assassin 在变身后从近战贴脸逻辑切到远程拉扯逻辑，没有影响其他近战刺客。
- 审查 `BattleSkillSystem`：确认新 target/effect 没有绕过既有直接目标合法性、射程、死亡、不可选中、自身施法等门禁。
- 审查 `BattleView` 和三个 animation driver：确认形态切换时销毁旧 driver、创建新 driver、方向默认值和远程攻击动画都能按新模型工作。
- 审查 `Stage01SampleContentBuilder` 与 `Assets/Data/Stage01Demo/...` 的一致性；这轮 Unity 生成器没有正常完成，资产是按现有 YAML 模板手工补齐的。
- 当前工作区仍有未提交/外部改动：`docs/planning/stage-01-hero-spec-decisions.md`、`docs/heroes/tank/blastshield.md`、`docs/today_work/0427.md`；评审 Demon commit 时不要混入这些改动。

### 验证情况
- 已执行 `git diff --check`，无空白错误。
- 已执行 Unity batch demo 内容生成两次：两次均未自然退出，停在 Unity 初始化/域重载之后；已停止本轮启动的 Unity 进程。
- Unity 日志 `game/Logs/DemonGenerateDemoContentRetry.log` 中有 `*** Tundra build success (0.92 seconds)`，且未检出 `error CS` / `Build failed` / `Exception`；这只能证明脚本编译成功，不等同于 demo 生成完整成功。
- 因生成器未正常落盘，已按现有 `.asset` 模板手工补齐 Demon 的 hero/skill/catalog asset。
- 未执行 Demon 专项战斗 smoke；后续建议用包含 Demon 的阵容验证：小技能交换最远后排、变身后模型切换、远程弹道、远程拉扯、死亡后回到基础形态。

## [16:02] Sniper 射手实现交接

### 本次做了什么
- 根据 `docs/heroes/marksman/sniper.md` 开始实现新射手英雄 `Sniper / 狙击手`。
- 新增 Sniper 的 Stage01 demo hero/skill 资产，包含小技能 `Deadeye Shot` 与大招 `Kill Zone`，并把 Sniper 加入 `Stage01HeroCatalog.asset`。
- 在 `Stage01SampleContentBuilder.cs` 中接入 Sniper 的生成逻辑、技能 owner override、复用 Rifleman 模型/弹道占位和英雄目录注册。
- 扩展共享连续动作序列：新增 `RefreshEveryIterationUniqueTarget`，用于“一次序列中每个目标最多执行一次”。
- 为唯一目标序列补了开始时目标快照：大招开始时按距离从远到近收集合法敌人，后续每枪只从快照里选择尚未执行、当前仍合法的目标；目标死亡、不可选中或超出合法条件时会跳过。
- 扩展 `FarthestEnemyFromSelf` 的序列排除能力，使 Sniper 小技能和大招都走共享目标选择，而不是写英雄私有循环。
- 对 Sniper 文档做了实现对齐：当前落地使用共享 `FarthestEnemyFromSelf` 策略，大招释放门槛按全图敌人数模板处理。
- 本轮没有提交 commit：当前工作区已有 Blastshield / reactive-counter 等未提交改动，且 `BattleSkillSystem.cs`、`Stage01SampleContentBuilder.cs` 与 Sniper 改动重叠，直接整文件提交会混入无关内容。

### 涉及文件
- `docs/heroes/marksman/sniper.md`
- `game/Assets/Scripts/Data/CombatActionSequenceData.cs`
- `game/Assets/Scripts/Heroes/RuntimeCombatActionSequence.cs`
- `game/Assets/Scripts/Battle/BattleAiDirector.cs`
- `game/Assets/Scripts/Battle/BattleCombatActionSequenceSystem.cs`
- `game/Assets/Scripts/Battle/BattleSkillSystem.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Data/Stage01Demo/Heroes/marksman_005_sniper/`
- `game/Assets/Data/Stage01Demo/Skills/marksman_005_sniper/`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`

### 需要检查什么
- 检查 `Deadeye Shot` 是否只在 `24` 范围内选择距离 Sniper 最远的合法敌人，且不会退化成继续集火最近前排。
- 检查 `Kill Zone` 是否符合文档语义：开始时快照敌方英雄，按远到近排序，每名敌方英雄最多被狙击一次，目标失效时跳过，硬控或强制移动会中断。
- 检查 `RefreshEveryIterationUniqueTarget` 是否没有破坏已有序列模式：普通 `KeepCurrentTarget`、`RefreshOnInvalid`、`RefreshEveryIteration` 应保持旧行为。
- 检查 `RuntimeCombatActionSequence.RestoreQueuedExecution()` 对已登记目标的回滚是否正确，避免队列失败后错误消耗唯一目标。
- 检查手工新增的 Sniper `.asset` 与 `Stage01SampleContentBuilder` 是否一致：基础射程 `12`、攻速约 `1.80 秒/次`、小技能倍率 `2.35`、大招倍率 `3.05`、大招序列 `4` 次补枪、射程 `56`。
- 当前工作区还有已存在或并行产生的 Blastshield / reactive-counter / Demon / Shadowstep 相关改动；评审 Sniper 时不要把这些内容误归到本轮。

### 给评审 AI 的重点
- 优先审查 `game/Assets/Scripts/Heroes/RuntimeCombatActionSequence.cs` 与 `game/Assets/Scripts/Battle/BattleCombatActionSequenceSystem.cs`：确认唯一目标快照的创建、排序、执行记录、跳过和中断逻辑不会残留状态。
- 重点审查 `game/Assets/Scripts/Battle/BattleSkillSystem.cs`：确认序列技能重新选目标时会优先使用快照目标，并且仍通过 `IsPrimaryTargetStillValidForCastRange`、直接目标合法性、射程和最小距离门禁。
- 检查 `game/Assets/Scripts/Battle/BattleAiDirector.cs`：确认 `FarthestEnemyFromSelf` 的排序 tie-break 仍按距离优先，再按低血目标处理，并能排除序列已执行目标。
- 检查 `Stage01SampleContentBuilder.cs` 的 Sniper 片段是否与现有英雄生成风格一致，特别是它和当前 Blastshield 改动在同一个文件内，后续提交时需要小心拆分或统一确认范围。
- 检查 `Stage01HeroCatalog.asset`、Sniper hero asset、两个 skill asset 的 GUID 引用是否能在 Unity 中解析。
- 对照 `docs/heroes/marksman/sniper.md`，确认实现没有把普攻也改成最远敌人；Sniper 的反后排能力应主要来自小技能和一局一次大招。

### 验证情况
- 已执行 `git diff --check`，Sniper 相关文件无空白错误；Git 仅提示部分文件下次触碰会从 LF 规范为 CRLF。
- 已执行 GUID / 资产引用检索，Sniper hero、skill、目录 meta 和 catalog 引用能互相对应。
- 已执行 Sniper 相关关键字检索，确认 `marksman_005_sniper`、`skill_sniper_*`、`RefreshEveryIterationUniqueTarget`、快照选择入口已落到代码和资产中。
- 尝试使用 `C:\Program Files\Unity 6000.3.13f1\Editor\Unity.exe` 对 `G:\BaiduNetdiskDownload\fight\game` 跑独立 batchmode 编译，日志 `game/Logs/codex_unity_compile_sniper.log` 只到项目路径切换后退出，日志尾部写有 `Application will terminate with return code 1`，不能视为完整 Unity 验证通过。
- 阻塞原因：同一项目已有 Unity batch 进程在跑 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentBatch`，日志 `game/Logs/codex_blastshield_generate_third.log`；该日志中可见 `Tundra build success`、无 `error CS`，并导入了 Sniper 目录，但缺少最终 batch 成功退出标记。
- 未执行 Sniper 专项战斗 smoke；后续建议用包含后排法师/辅助/射手的红方阵容验证小技能点名最远目标、大招每人最多一枪、目标死亡跳过和被硬控中断。

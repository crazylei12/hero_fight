## [14:26] 僧侣实现与交接

### 本次做了什么
- 基于 `docs/heroes/support/monk.md` 完成了新辅助英雄“僧侣”的实现接入，并先按用户最新改动对齐了设计意图。
- 确认了本轮用户对 `monk.md` 的关键修改：小技能去掉人数/残血门槛，改为冷却好了就放；大招友军人数门槛从 3 降到 2；大招去掉“友军正在贴身交战”触发，保留低血量触发为主。
- 在战斗 AI 中补了“近战辅助”分支，避免所有 `Support` 都按后排站位处理，使僧侣能混在前排近战群里行动。
- 在 `Stage01SampleContentBuilder` 中新增 Monk 的英雄、主动技能、终极技能、基础攻击、终极释放条件、资源校验与英雄目录注册逻辑。
- 手动补齐了 Monk 的 `HeroDefinition`、两个 `SkillData` 资源和 `Stage01HeroCatalog` 引用，因为 Unity 批处理生成被项目内已有第三方插件编译错误阻塞，未能自动产出资源。
- 已完成一次可交接提交：`e90112a support: add monk frontline healer`。

### 涉及文件
- `docs/heroes/support/monk.md`
- `game/Assets/Scripts/Battle/BattleAiDirector.cs`
- `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs`
- `game/Assets/Resources/Stage01Demo/Stage01HeroCatalog.asset`
- `game/Assets/Data/Stage01Demo/Heroes/support_003_monk/Monk.asset`
- `game/Assets/Data/Stage01Demo/Skills/support_003_monk/Renewing Pulse.asset`
- `game/Assets/Data/Stage01Demo/Skills/support_003_monk/Guardian Mantra.asset`

### 需要检查什么
- 检查 `monk.md` 中的小技能/大招规则，是否与代码和资源参数完全一致。
- 检查 `BattleAiDirector.cs` 对 `HeroClass.Support` 的改动，确认只影响近战辅助，不会误伤原有后排辅助站位。
- 检查 `Stage01SampleContentBuilder.cs` 里的 Monk 构建逻辑与手动写入的资源是否一致，避免后续批量重建内容时出现漂移。
- 检查 `Stage01HeroCatalog.asset` 是否正确包含 Monk，且没有重复、丢失或引用错误。
- 本轮没有完成一次干净的 Unity 全项目编译，也没有实际跑战斗局内表现；这些都需要后续补查。

### 给评审 AI 的重点
- 优先看 `game/Assets/Scripts/Battle/BattleAiDirector.cs`，确认“近战辅助不再按后排辅助处理”的条件是否足够稳，不会影响现有辅助英雄行为。
- 重点看 `game/Assets/Scripts/Editor/Stage01SampleContentBuilder.cs` 与 `docs/heroes/support/monk.md` 的一致性，特别是：
- 小技能是否为以自身为中心的群体回血，半径 `4.5`，冷却 `8`，无 ally-count/残血释放门槛。
- 大招是否为以自身为中心的大范围护盾，半径 `6.8`，至少 `2` 名友军在范围内且至少 `2` 名友军生命低于 `70%` 时释放，`45s` 后降级兜底。
- 检查 `game/Assets/Data/Stage01Demo/Heroes/support_003_monk/` 和 `game/Assets/Data/Stage01Demo/Skills/support_003_monk/` 下手写资源是否和 builder 输出预期一致，避免后续被编辑器重写时出现字段缺失或 GUID 问题。
- 结合 `AGENTS.md` 与 `game/AGENTS.md` 复核：这次实现是否仍符合第一阶段最小自动战斗原型范围，没有额外扩阶段。

### 验证情况
- 已核对提交存在：`e90112a support: add monk frontline healer`。
- 已确认当前工作区为干净状态，无未提交改动。
- 已尝试通过 Unity batchmode 执行 `Fight.Editor.Stage01SampleContentBuilder.GenerateDemoContentBatch` 生成/校验样例内容。
- Unity 运行被项目内已有第三方插件编译错误阻塞，主要是 `UnityEngine.UI`、`EventSystems`、`TMPro`、`InputSystem` 等依赖缺失，故未完成整项目编译验证。
- 本轮检查日志时，没有看到 `BattleAiDirector.cs` 与 `Stage01SampleContentBuilder.cs` 命中错误列表，但这不等于完整通过编译。
- 未做局内实机战斗验证，僧侣的实际站位、回血覆盖和护盾释放时机仍需进入场景后补验。
